# StepQuest — 구현 계획서 v3

> 현실의 걸음으로 움직이는, 느린 도트 감성 RPG (Android 전용)

---

## 0. 이 문서에 대하여

v2에 달아주신 `->` 코멘트 **32개를 전부 반영**한 버전입니다.

v2와 달라진 점의 성격:

- v2는 "게임을 다시 설계"한 문서였습니다.
- **v3는 "숫자와 규칙을 확정"한 문서입니다.** 이제 애매한 표현 대신 실제로 코드에 박을 수 있는 값이 들어 있습니다.
- 그리고 **§9에 태스크별 실행 프롬프트**가 있습니다. 앞으로는 `T4 진행해` 한 마디면 됩니다. (질문 3번 답변)

읽는 순서 추천:

1. **§3 — 물어보신 것에 대한 제 답변** (길드/랭킹, 어뷰징, 드라이브 vs Supabase, 나침반, 수동 스탯의 함정)
2. **§2 반영표** — 내 코멘트 32개가 어디로 갔는지
3. **§4~5** — 확정된 규칙과 숫자
4. **§8~9** — 태스크 목록 + 실행 프롬프트
5. **§11** — 답이 필요한 질문 (여기만 답 주시면 됩니다)
6. **§13** — v2→v3 변경 요약

**현재 진행 상황: T1, T2, T3 완료. 실기기에서 동작 확인됨.** (§8 참조)

---

## 1. 게임 한 줄 정의

> **걸어서 번 "WP"로 하루 10~20분, 몬스터 몇 십 마리 잡고, 천천히 강해지는 도트 RPG.**

### 1.1. 하루 플레이 시나리오 (목표: 1일 10~20분)

```
08:20  출근길. 앱 안 켬. 폰 센서가 걸음만 셈.
19:40  퇴근길 지하철. 앱 실행.
       ├ Health Connect 읽기 → "오늘 10,240보 → WP 10,240" (+ 자정 기본 지급 1,000)
       ├ HP 412/680. 어제 싸우다 남은 체력 + 10분당 1%씩 자연회복된 만큼.
       ├ 마을: 여관 250골드로 완회복 vs 물약 3개 480골드. 물약 선택.
       ├ 창고에 골드 8,000 예치 (수수료 2% = 160골드). 죽어도 이건 안 날아감.
       ├ [지역 3: 메마른 협곡] → [사냥터: 마른 강바닥] 입장 (입장료 없음)
       │   [조우] 탭 → WP 363 소모 → 15틱(약 18초) 전투
       │   "모래도마뱀에게 34의 피해를 입혔다"
       │   승리. 골드 84, EXP 41, 도마뱀 가죽 1.
       ├ [연속 사냥] 켜고 20전투 자동 진행 (HP 30% 이하면 자동 정지)
       ├ 중간에 물약 1개 사용, 레벨업 1회 → 스탯 포인트 3점 수동 배분
       └ 남은 WP 3,600은 내일로 이월 (보유 상한 없음)
19:57  앱 종료.
```

핵심은 **"못 걸으면 못 논다"** 입니다. 시간이 흐른다고 캐릭터가 강해지지 않습니다.
(예외: HP만 10분당 1%씩 회복됩니다. HP는 "행동력"이 아니라 "지속력"이라서 괜찮습니다.)

### 1.2. 설계 원칙 4개

| 원칙 | 의미 | 이걸로 잘려나간 것 |
| --- | --- | --- |
| **걸음이 유일한 수도꼭지** | 모든 성장은 전투를 거치고, 전투는 WP를 쓰고, WP는 걸음에서 나온다. 단 자정 기본 1,000 WP는 지급 | 시간 방치 보상, 오프라인 누적 골드 |
| **하루 10~20분** | 하루에 할 수 있는 행동 수 자체가 WP로 제한됨 | 24시간 자동 사냥, 일일 퀘스트 10종 |
| **천천히** | Lv10 = 1주, Lv30 = 1.5개월, Lv50 = 4개월 | 환생, 급성장 가챠 |
| **고전 도트 RPG** | 마을/지역/사냥터/여관/상점/창고. 지역은 레벨+보스로 해금 | 걸음 수에 따른 실시간 배경 전환 |

---

## 2. v2 코멘트 반영표 (32개)

| #  | v2에 달아주신 코멘트 | 반영 결과 | 위치 |
| -- | --- | --- | --- |
| 1  | 1일 3~6분은 짧다, 10~20분 목표 | 전투당 WP 비용을 1,000 → **300**으로 낮춤. 10,000보 = 하루 36전투 ≈ 11분 + 메뉴 조작 = **13~17분** | §6.4 |
| 2  | WP 보유 상한이 필요한가 | **삭제.** 대신 "연속 사냥 1회 20전투" 상한으로 자동파밍만 막음. 부작용은 §3.7에 명시 | §4.1, §3.7 |
| 3  | 기력 → WP (Walking Point) | 전 문서 **WP**로 통일. 코드 심볼도 `wp`, 파일명 `src/game/wp.ts` | 전체 |
| 4  | 자정 기본 지급 1,000은 있으면 좋겠다 | **유지.** "비 오는 날도 한 판은 한다"는 최소 보장선 | §4.1 |
| 5  | 길드/랭킹/친구추가 원함, 어뷰징 방지 난이도? | **§3.1에 전부 답변.** 결론: 친구 리더보드는 쉽고 안전 → 출시 후 1순위(T28). 길드는 여전히 보류 | §3.1 |
| 6  | 센서 1차 + Health Connect 보정, 워치 때문에 HC값이 튈 때 어뷰징 오탐 우려 | **§3.3에서 설계 확정.** `max(HC, 센서)` 단조 증가 방식. 두 값을 **비교해서 벌주는 로직을 아예 안 만들면** 이 우려 자체가 사라짐 | §3.3, §5.4 |
| 7  | 나침반이 뭐지 | **§3.5 답변.** v1의 일일 횟수 재화. v2에서 이미 삭제됨. WP가 그 역할 | §3.5 |
| 8  | 구글 로그인 → 본인 구글 드라이브 백업 | **§3.4에서 Supabase와 비교.** 결론: 친구 리더보드를 할 거면 Supabase 하나로 통일이 낫다 | §3.4 |
| 9  | Q1 → WP | 확정 | §4.1 |
| 10 | Q2 → (a) AI 생성, 수작업 없이 그냥 쓰기 목표 | 확정. §7.5에 "수작업 없이 톤 통일하는 법" 추가 | §7.5 |
| 11 | Q3 → (a) 골드 10% 상실 | 확정. 단 **창고 골드는 면제** (#18 반영) | §4.2 |
| 12 | Q4 → (a) 첫 외부 테스터 직전 | 확정. T25 | §8 |
| 13 | 사냥터 입장은 무료, **몬스터 조우마다 300 WP** | 확정. 사냥터는 "방", 조우가 과금 단위 | §4.1 |
| 14 | 지역당 사냥터 여러 개, 사냥터당 몬스터 여러 종 | 확정. 지역 5개 × 사냥터 3~4개 × 몬스터 3~5종 | §4.4 |
| 15 | 지역 이동 1,000 WP | 확정 (v2의 2,000에서 하향) | §4.1 |
| 16 | 던전 기획 미비, 주석 없음 | **S5로 미루고 "기획 선행 필요"로 명시.** T21 시작 전 별도 세션 | §8 |
| 17 | 틱 수는 몬스터/장비/스탯에 따라 달라짐, 고정 불가 | 확정. **목표 밴드**로만 표현: 적정 레벨 기준 지역1 10~20틱 → 지역5 18~32틱 | §4.2 |
| 18 | HP 자동 회복 10분당 전체의 1% | 확정. 오프라인에도 적용. 시계 조작 방어는 §4.2에 | §4.2 |
| 19 | 창고에 골드 예치, 수수료 2% | 확정. **단 입출금은 마을에서만** (이유는 §3.8) | §4.5 |
| 20 | 대미지 = ATK × K/(K+DEF) × rand(0.8~1.2), K=50 | 확정. 그대로 채택 | §4.2 |
| 21 | 크리티컬 확률은 별도 스탯 | 확정. `CRI`(확률) / `CRD`(배율) 분리 | §4.3 |
| 22 | 회피율도 스탯 | 확정. `EVA`. 회피 판정이 크리 판정보다 먼저 | §4.2, §4.3 |
| 23 | 스탯 수동 분배, 재분배는 WP로 | 확정. **레벨당 3포인트 수동 + 직업별 자동 성장 병행.** 이유는 §3.6 | §4.3 |
| 24 | 물약은 절대값 회복, %는 고급 | 확정. 소/중/대 = 절대값(지역별 판매), 엘릭서 = % | §4.5 |
| 25 | 같은 등급 장비라도 습득 시 품질 80~120% 랜덤 | 확정. 인벤토리 아이템을 **인스턴스**로 저장(세이브 스키마 변경 필요) | §4.5 |
| 26 | 강화는 %로, 1강당 +10% | 확정. 최종스탯 = 기본 × 품질 × (1 + 0.1 × 강화) | §4.5 | -> 강화 10프로는 복리로
| 27 | DAU 1,000 길드는 인정, 그런데 친구 경쟁은 있어야겠다 | **§3.1 답변.** 친구 리더보드가 그 욕구의 95%를 1/10 비용으로 해결 | §3.1 |
| 28 | 리더보드 있으면 좋겠다 | **친구 리더보드는 T28로 확정.** 글로벌 랭킹은 §11 Q1으로 질문 | §3.1, §11 |
| 29 | 5분마다 전송 말고, 접속 중엔 긴 텀 + 기본은 앱 켤 때 | 확정. 포그라운드 진입 시 1회 + 5분 스로틀. T25 이후 적용-> 5분 스로틀이 정확히 무슨 의미야 5분마다 서버 저장해? 아니면 5분마다는 로컬저장하고 앱 킬떄 서버 저장해? | §5.5 |
| 30 | 손으로 만드는 원형(archetype) 예시 달라 | **§7.2에 실제 JSON 예시 + 생성 결과까지** 전부 작성 | §7.2 |
| 31 | 수동 분배면 HP만 찍는 게 무조건 이득일 수도 | **진짜 문제 맞습니다. §3.6에서 3중 방어.** 핵심은 "전투 최대 60틱 → 무승부" | §3.6 |
| 32 | 10,000보 기준, Lv10=1주 / Lv30=1.5개월 / Lv50=4개월 | **§6에서 수식으로 역산 완료.** 실제 결과 **Lv10=7.0일 / Lv30=44.9일 / Lv50=120.1일** | §6.2 |

---

## 3. 물어보신 것에 대한 제 답변

### 3.1. 길드 / 랭킹 / 친구추가 — 뭘 어떻게 하는 게 좋을까 (#5, #27, #28)

**먼저 질문을 정확히 쪼개면 3개입니다.**

| 원하는 것 | 실제 욕구 | 구현 비용 | 위험 |
| --- | --- | --- | --- |
| 친구 추가 + 친구 리더보드 | "친구 걸음/레벨 보고 자극받기" | **낮음** (테이블 2개, 화면 1개) | **거의 없음** |
| 글로벌 랭킹 | "내가 전체 몇 등인지" | 중간 | **높음** (조작판 되기 쉬움) |
| 길드 | "같이 뭘 하기" | **높음** (서버 트랜잭션, 채팅, 운영) | 콜드스타트 |

**제 결론: 친구 리더보드만 먼저, 글로벌 랭킹은 그 다음, 길드는 여전히 보류.**

이유:

1. **"친구들 걸음수나 레벨을 보려면 그게 길드나 마찬가지"** — 맞는 말씀인데, 순서가 반대입니다.
   길드는 "친구 목록 + 공유 목표 + 채팅 + 역할/권한 + 가입/탈퇴 + 운영"입니다.
   원하시는 건 그중 **첫 번째 하나**고, 그건 길드 없이 됩니다.

2. **친구 리더보드는 조작에 강합니다.** 전체 랭킹 1등이 흔들이 기계면 게임이 망하지만,
   내 친구 5명 중 1명이 조작하면 **내가 친구를 끊으면 끝**입니다. 판이 안 망가집니다.
   즉 어뷰징 방지가 "필수"가 아니라 "있으면 좋은 것"이 됩니다. 이게 결정적입니다.

3. **길드는 DAU가 붙기 전엔 빈 방만 생깁니다.** 이건 v2 판단 그대로입니다.

**그래서 T28 하나로 정리했습니다:** 친구 코드 발급 → 친구 추가 → 친구 목록에 `오늘 걸음 / 레벨 / 마지막 접속` 표시 + **주간 걸음 합계 순위**. 서버는 Supabase 테이블 2개(`profiles`, `friendships`)면 끝납니다.

### 3.2. 어뷰징 방지, 구현이 어려운 편인가? (#5)

**최소 버전은 정말 쉽습니다. 서버 코드 세 줄 수준입니다.**

```sql
-- 걸음 제출 시 서버에서 거부하는 조건 3개
1. steps_today > 60000                      → 거부   (하루 상한. 60km 걷는 사람 없음)
2. (steps - prev_steps) / 경과분 > 250      → 거부   (분당 250보 = 시속 12km 달리기 수준)
3. 클라이언트가 보낸 timestamp는 전부 무시, 서버 now()만 사용
```

이게 전부입니다. "케이던스 분석", "가속도 패턴 학습" 같은 건 안 합니다.
위 3줄로 **흔들이 기계(분당 300~600보를 24시간 유지)는 전부 걸립니다.**

**그리고 걱정하신 워치/배드민턴 케이스 — 이건 아예 문제가 안 되게 설계했습니다.**

> 걱정: "폰은 가만히 있는데 갤럭시워치가 걸음을 세면, 센서값 << HC값이라 어뷰징으로 잡히지 않을까?"

**답: 센서값과 HC값을 비교해서 벌주는 로직을 만들지 않으면 됩니다.**

- 서버에 제출하는 값은 **Health Connect 값 하나뿐**입니다.
- 센서는 "앱 켜놓고 걸을 때 숫자가 즉시 올라가는 연출"에만 씁니다. 서버로 안 갑니다.
- 어뷰징 체크는 **HC 값의 시간당 증가율**만 봅니다. 배드민턴을 아무리 열심히 해도 분당 250보는 안 나옵니다.

즉 "두 소스가 다르다" 자체를 **정상**으로 취급합니다. 그래야 워치 유저가 손해를 안 봅니다.

### 3.3. 센서 + Health Connect 이중 소스 설계 확정 (#6)

말씀하신 구조가 맞습니다. 다만 "보정"이라는 단어를 **`max()`** 한 줄로 바꾸면 전부 해결됩니다.

```ts
// 앱이 켜져 있는 동안
sensorLive   = Pedometer.watchStepCount()   // 구독 시작 이후 증가분. 즉시 반영
// 앱 켤 때 / 새로고침 버튼
hcToday      = HealthConnect.readTodaySteps()  // 확정값. 수 분 지연

todaySteps   = Math.max(hcToday, sensorBaseline + sensorLive)
grantedWp   += Math.max(0, todaySteps - alreadyGrantedToday)   // 단조 증가. 절대 안 깎임
alreadyGrantedToday = todaySteps
```

이 설계가 좋은 이유:

| 상황 | `max()`가 하는 일 |
| --- | --- |
| HC가 아직 동기화 안 됨 (센서 > HC) | 센서값 사용 → 즉시 보상 |
| 워치로 걸음 (HC > 센서) | HC값 사용 → 워치 걸음도 인정 |
| HC가 나중에 따라잡음 | 차액만 추가 지급. 중복 지급 없음 |
| 폰 재부팅 (센서 카운터 리셋) | `watchStepCount`는 구독 시점부터의 증가분이라 영향 없음 |
| HC가 뒤로 감 (데이터 삭제 등) | `Math.max(0, ...)` 때문에 WP가 깎이지 않음 |

**필요한 권한:** `ACTIVITY_RECOGNITION` (센서), Health Connect 읽기 권한 (걸음).
**백그라운드 권한은 여전히 불필요합니다.** 앱이 꺼져 있는 동안은 HC가 알아서 쌓아두고, 켤 때 한 번 읽습니다.

### 3.4. 구글 드라이브 백업 vs Supabase — 뭐가 다르고 뭐가 나은가 (#8)

| | **구글 드라이브 `appDataFolder`** | **Supabase (Auth + Postgres)** |
| --- | --- | --- |
| 데이터가 어디 있나 | **유저 본인 드라이브** (앱 전용 숨김 폴더) | **내 DB** |
| 저장 비용 | 영원히 0원 | 무료 티어 내 0원, 넘으면 과금 |
| 개인정보 책임 | 거의 없음 (내가 보관 안 함) | **있음** (처리방침에 보관 명시 필요) |
| 구현 난이도 | 생각보다 잔손질 많음 — OAuth 토큰 갱신, 기기 2대 충돌 처리, Drive API 쿼터 | SDK가 대부분 처리. **더 쉬움** |
| 세이브 조작 | 유저가 파일을 볼 수 있고 고칠 수 있음 | 서버에서 검증 가능 |
| **친구 리더보드로 확장** | **불가능.** 소셜용 백엔드를 따로 또 만들어야 함 | **그대로 확장.** 같은 계정에 테이블만 추가 |

**제 추천: Supabase 하나로 통일.**

결정적인 이유는 마지막 줄입니다. 친구 리더보드를 하고 싶다고 하셨는데(#27, #28), 그러려면
**어차피 계정 체계가 필요**합니다. 계정이 있으면 세이브 1행 얹는 건 거의 공짜입니다.
드라이브 백업을 먼저 만들면 나중에 Supabase를 또 붙이고 **백업 경로가 두 개**가 됩니다. 최악입니다.

Supabase 선택도 동의합니다 — 랭킹이 `ORDER BY ... LIMIT 100` 한 줄이고 비용 예측이 쉽습니다.

**다만 T25(클라우드 세이브)까지는 아직 멉니다.** 그 전에 세이브가 날아가는 게 걱정되시면,
**T3.5로 "세이브 파일 내보내기/가져오기"를 1시간이면 붙일 수 있습니다.** (`expo-sharing` + `expo-document-picker`, 백엔드 0) -> 클라우드 세이브가 필요해? supabase로 모든 세이브를 할 수 없나?  지금 너도 supabase로 통일하라는데 클라우드 세이브가 필요한지
필요하시면 §11 Q7에 답 주세요.

### 3.5. 나침반이 뭐였나 (#7)

**v1(옛날 계획서)에 있던 일일 횟수 재화입니다.** 모바일 게임의 "하트" 같은 것으로,
하루 최대 10개까지만 모이고 행동할 때마다 1개씩 썼습니다.

v1의 문제는 **10개가 곧 10,000보**여서, 15,000보 걷는 사람의 초과 5,000보가 그냥 버려진다는 것이었습니다.
**v2에서 이미 삭제됐고, WP가 그 역할을 대신합니다.** v3에는 이 단어가 안 나옵니다. 잊으셔도 됩니다.

### 3.6. 수동 스탯 분배의 함정과 해결 (#23, #31)

> "hp 많은게 무조건적으로 좋을수도 있고 그러니까 잘 생각해야겠네"

**정확한 지적이고, 실제로 그렇게 됩니다.** 이유를 먼저 정리하면:

전투에 제한 시간이 없으면 **HP만 찍은 캐릭터는 절대 안 죽고**, 공격력이 낮아도 언젠가는 이깁니다.
"느리지만 100% 승리"가 "빠르지만 가끔 패배"를 이깁니다. 다른 스탯이 전부 죽은 선택이 됩니다.

**3중 방어로 막았습니다:**

**① 전투 최대 60틱 → 무승부(`timeout`)**
60틱(약 72초) 안에 못 이기면 전투 종료, **보상 없음, WP 환불 없음**, HP는 남은 만큼 유지.
이 한 줄이 "탱킹만 하면 이긴다"를 완전히 차단합니다. 엔진 무한루프 방지도 덤입니다.
-> 이건 없애줘 기본적으로 게임 내에서 물약을 먹는다고 쳐도 재화가 소모되는것이고 hp를 많이 찍었다면 그만큼 다른 스텟이 저조하니까 밸런스 맞을것이야   만약 이후에 흡혈 아이템같은게 있어서 서로 못끝낸다고 하더라도 그럴땐 본인 대미지 스펙이 안되는것이니까 도망가는것이 맞는것이고

**② 1차 스탯 → 파생 스탯 매핑 (HP를 직접 못 찍음)**
`체력`에 투자하면 HP가 오르지만, HP 외에는 아무것도 안 오릅니다.
`힘`은 ATK만, `민첩`은 SPD/회피만, `행운`은 크리/드랍만 올립니다. 각자 고유한 역할이 있습니다.

**③ 직업별 자동 성장이 바닥을 깔아줌**
레벨업 시 **직업 고정 성장(자동) + 3포인트(수동)** 둘 다 받습니다.
자동 성장이 최소한의 HP/ATK를 보장하므로, 수동 배분을 아무리 이상하게 해도 **플레이 불가능한 캐릭터는 안 나옵니다.**
동시에 밸런싱할 때 "최소 스탯 라인"이 확정되므로 시뮬레이터(T12)가 의미 있는 숫자를 냅니다.

**재분배 비용:** 3,000 WP. **단 Lv10 이전에는 무료** (초반에 모르고 찍은 건 봐줍니다).

### 3.7. WP 보유 상한을 없애면 생기는 일 (#2)

상한 삭제, 요청대로 합니다. 다만 부작용 하나는 알고 계셔야 합니다.

**두 달 동안 앱을 안 켜고 매일 걸으면 약 66만 WP가 쌓입니다.** 돌아와서 2,200전투를 할 수 있습니다.
이론상 11시간 분량인데, **연속 사냥을 켜두면 실제로 가능합니다.** 그러면 이 게임이 거부한 "방치형"이 됩니다.

**해결: 상한 대신 연속 사냥에 1회 20전투 제한.**
20전투마다 버튼을 다시 눌러야 하므로 "틀어놓고 딴 짓"이 안 됩니다.
돌아온 유저가 WP 폭탄을 즐겁게 쓰는 건 그대로 두되, 무인 파밍만 막는 구조입니다.

(만약 이것도 귀찮다고 느껴지면 상한을 100,000 WP(약 9일치)로 두는 안이 있습니다. §11 Q4)
-> 궁금증1. 안켜도 wp가 늘어? 매일 접속을 했을때 그날의 걸음수 기반으로 wp가 생기는거지 않나
내의견: 그리고 연속 사냥 제한은 없앨거야 연속은 가능하게끔

### 3.8. 창고 수수료 2%가 그냥 세금이 되는 문제 (#19)

말씀하신 대로면 **예치 수수료 2% < 사망 페널티 10%** 라서, 최적 플레이는 항상 "번 즉시 전액 예치"입니다.
그러면 창고는 선택이 아니라 **매번 눌러야 하는 잡일**이 됩니다.

**해결: 입출금은 마을에서만.**
사냥터에 나가기 전에 "물약 살 돈 얼마를 들고 갈까"를 정해야 합니다.
현금을 많이 들고 가면 죽을 때 손해가 크고, 적게 들고 가면 물약이 모자랍니다.
**2% 수수료가 비로소 의미 있는 선택**이 됩니다. 고전 RPG의 좋은 긴장감입니다.

동의 안 하시면 §11 Q3에서 "아무 데서나"로 바꿀 수 있습니다.
-> 동의 안함  물약은 아무곳에서나 살 수 있음 
창고 넣을떄 수수료 2%는 유지하고 창고에 넣을 수 있는 최대 금액이 처음에는 적은데 이것을 늘리는것도 골드로 하면 좋아보인다
---

## 4. 코어 루프 — 확정 규칙

### 4.1. WP (Walking Point)

```
1 걸음 = 1 WP
자정 기본 지급 = 1,000 WP        (마지막 수령 시각의 단조 증가로 판정. 시계 뒤로 돌려도 중복 지급 없음)
획득 상한 = 없음
보유 상한 = 없음                  (§3.7 참조)
```

**WP 소비처 (확정)**

| 용처 | 비용 | 설명 |
| --- | --- | --- |
| **사냥터 입장** | **0** | 사냥터는 "방". 들어가는 건 공짜 |
| **몬스터 조우** | **300 × 1.1^(r-1)** | 주 소비처. r1:300 r2:330 r3:363 r4:399 r5:439 |
| **지역 보스 조우** | **1,000 × 1.1^(r-1)** | 해금용. 실패해도 재도전 가능-> 해금은 10000× 1.1^(r-1)로 가고 조우는 3000 × 1.1^(r-1)로 하자 | 
| **지역 이동** | **1,000** | 새 지역 첫 진입 시 1회만 (영구 해금)-> 아니야 이동할떄마다 1000 들게 |
| **스탯 재분배** | **3,000** | Lv10 이전 무료 |
| **던전 입장** | 미정 | 기획 선행 필요 (T21) |

**하루 행동 횟수 (지역 1 기준)**

| 오늘 걸음 | 보유 WP | 조우 가능 수 | 체감 |
| --- | --- | --- | --- |
| 0보 (비 오는 날) | 1,000 | 3회 | "몇 판은 하고 잔다" |
| 3,000보 | 4,000 | 13회 | 가볍게 |
| **10,000보 (기준선)** | **11,000** | **36회 ≈ 11분** | **기준선** |
| 15,000보 | 16,000 | 53회 | 남는 건 내일로 이월 |

### 4.2. 전투

```
틱 = 1.0초에 1행동 (연출 포함 ~1.2초)
행동 순서 = SPD 높은 순, 동률이면 플레이어 우선
최대 틱 = 60  → 초과 시 무승부. 보상 없음, WP 환불 없음, HP는 남은 만큼 유지 -> 위에 말했지만 무승부 제거
```

**목표 전투 길이** (적정 레벨 몬스터 기준. 장비/스탯/몬스터에 따라 달라지는 건 당연 — #17)

| 지역 | 목표 틱 | 실시간 |
| --- | --- | --- |
| 1 | 10~20 | 12~24초 |
| 3 | 14~26 | 17~31초 |
| 5 | 18~32 | 22~38초 |

**대미지 수식 (확정 — #20, #21, #22)**

```ts
// 1) 회피 판정이 먼저
if (random() < target.EVA) → "빗나갔다", 대미지 0, 턴 종료

// 2) 기본 대미지 (K = 50)
피해배율   = 50 / (50 + target.DEF)
기본대미지 = attacker.ATK × 피해배율 × random(0.8, 1.2)

// 3) 크리티컬은 별도 확률 스탯
if (random() < attacker.CRI) → 기본대미지 × attacker.CRD   // "치명타!"

// 4) 최소 보장
최종 = max(1, floor(기본대미지))
```

`K = 50`의 감쇠 곡선 — 초반 한 자리 DEF부터 후반 세 자리 DEF까지 자연스럽습니다:

| DEF | 5 | 20 | 50 | 100 | 200 |
| --- | --- | --- | --- | --- | --- |
| 피해배율 | 0.91 | 0.71 | 0.50 | 0.33 | 0.20 |

**HP 규칙 (#18)**

- HP는 전투가 끝나도 **회복되지 않고 그대로 유지**. 세이브에 저장됩니다.
- **자연 회복: 10분당 최대 HP의 1%.** 0 → 만피까지 약 16.7시간.
  - 앱이 꺼져 있어도 적용 (`hpUpdatedAt` 저장 후 다음 실행 시 경과 시간으로 계산)
  - **시계 조작 방어:** 경과 시간이 음수면 회복 0, 1회 계산에 최대 24시간까지만 인정
- 빠른 회복 수단 둘: **물약**(즉시, 골드) / **여관**(전체 회복, 골드)
- 결과: **골드 = 지속력**, 골드는 전투에서만 나오고, 전투는 WP를 쓰고, WP는 걸음에서 나옵니다.

**전투 결과 4종**

| 결과 | 처리 |
| --- | --- |
| `win` | 골드 + EXP + 드랍. HP 남은 만큼 유지 |
| `flee` | 항상 성공. 보상 없음, WP 환불 없음. **HP 보존** |
| `lose` (HP 0) | 마을 귀환. **소지 골드 10% 상실 (창고 골드는 면제)**. HP는 최대의 10%로 회복. 장비 손실 없음 |
| `timeout` (60틱) | 무승부. 보상 없음, WP 환불 없음. HP 보존 |

### 4.3. 스탯과 성장 (#23, #31)

**1차 스탯 — 레벨당 3포인트 수동 배분**

| 1차 스탯 | 1포인트당 |
| --- | --- |
| **힘 STR** | ATK +2 |
| **체력 VIT** | maxHP +10, DEF +0.5 |
| **민첩 AGI** | SPD +1.5, EVA +0.15%p -> 민첩에서 spd가 아무리 높아봤자 선빵이 끝이라서 큰 메리트가 없어 보여 선빵과 함께 초당 공격횟수도 영향을 끼치자 차이가 많이 나면 적이 10번 떄릴떄 나는 13번 때릴 수 있는 식으로|
| **행운 LUK** | CRI +0.25%p, 드랍률 +0.2%p |

**직업별 자동 성장 — 레벨당 (배분 불가, 바닥을 깔아주는 역할)**

| 직업 | maxHP | ATK | DEF | SPD | 성격 |
| --- | --- | --- | --- | --- | --- |
| 전사 | +14 | +2.0 | +1.5 | +0.8 | 오래 버팀 |
| 도적 | +8 | +2.5 | +0.8 | +1.6 | 먼저 때리고 크리 |
| 마법사 | +7 | +3.0 | +0.6 | +1.0 | 한 방이 큼 |

**Lv1 기본값:** maxHP 100, ATK 10, DEF 5, SPD 10, CRI 5%, CRD 1.5×, EVA 3%

**EXP → 레벨:** §6.2

### 4.4. 지역과 사냥터 (#14)

```
지역(Region)   ← 고정 순서. 해금 = 직전 지역 보스 클리어 + 권장 레벨 도달. 이동 1,000 WP. 영구 해금
사냥터(Field)  ← 지역 안의 "방". 입장 무료. 각 사냥터마다 나오는 몬스터 종류가 다름
조우(Encounter)← 사냥터 안에서 버튼. 해당 사냥터의 몬스터 풀에서 가중 랜덤. 300 WP
```

**MVP 지역 5개**

| # | 지역 | 권장 Lv | 사냥터 | 사냥터당 몬스터 | 보스 |
| --- | --- | --- | --- | --- | --- |
| 1 | 바람 부는 초원 | 1~8 | 3 | 3종 | 들개 우두머리 |
| 2 | 이끼 낀 동굴 | 8~16 | 3 | 4종 | 동굴 거미 여왕 |
| 3 | 메마른 협곡 | 16~26 | 4 | 4종 | 모래 골렘 |
| 4 | 잿빛 화산길 | 26~38 | 4 | 5종 | 화염 드레이크 |
| 5 | 서리 봉우리 | 38~50 | 4 | 5종 | 서리 군주 |

합계 **몬스터 78종 + 보스 5종**. §7의 생성 파이프라인으로 뽑습니다.

**연속 사냥 (#2 부작용 대응)**

사냥터에서 `[연속 사냥]` 토글 → 조건에 걸릴 때까지 자동으로 조우 반복.

```
자동 정지 조건: WP 부족 / HP가 설정값(기본 30%) 이하 / 인벤토리 가득 / 20전투 도달 ->20전투 도달같은건 없애,  hp가 설정값 이하인 옵션은 없애줘 그래야지 쪼이는 맛이 있으면서 계속 보지 hp 적을때 물약 먹고 유저의 실시간 개입이 있지. 자동 정지 조건은 인벤토리 가득일때 뿐이야
```

### 4.5. 경제

**물약 (#24) — 절대값 회복이 기본, %는 고급**

| 아이템 | 회복 | 가격 | 판매 지역 |
| --- | --- | --- | --- |
| 물약(소) | HP **+60** | 60골드 | 지역 1~ |
| 물약(중) | HP **+200** | 180골드 | 지역 2~ |
| 물약(대) | HP **+600** | 480골드 | 지역 4~ |
| 엘릭서 | HP **50%** | 1,200골드 | 드랍 + 지역 5 한정 판매 |

지역이 올라가면 상위 물약이 열리므로, 절대값이어도 후반에 무의미해지지 않습니다.

**여관:** `150 × 1.45^(r-1)` 골드, HP 전체 회복. (r1:150 r3:315 r5:663)

**창고 (#19, §3.8):** 입금 시 **2% 수수료**, 출금 무료. **입출금은 마을에서만.** 사망 시 창고 골드 면제.

**장비 품질 (#25) — 같은 등급이라도 개체차**

```
드랍/구매 시 quality = 0.8 ~ 1.2 (삼각분포. 1.0 근처가 흔하고 양 끝이 드묾)
인벤토리 표시: "기사의 장검 (114%)"
```

> **세이브 스키마 영향:** 인벤토리는 아이템 **정의 ID**가 아니라 **인스턴스**를 저장해야 합니다.
> `{ uid, defId, quality, enhance }` — T13에서 세이브 v2 마이그레이션이 필요합니다. (마이그레이션 체인은 T3에 이미 준비됨)

**장비 강화 (#26)**

```
최종 스탯 = 기본스탯 × quality × (1 + 0.10 × 강화단계) -> 1.1^강화단계로 가자 그래야지 고 강화의 메리트가 훨씬 크지
강화 상한 = +10
비용      = 장비가격 × 0.3 × 1.6^N
성공률    = +1~3: 100% / +4: 90% / +5: 80% / +6: 70% / +7: 60% / +8: 50% / +9: 40% / +10: 30%
-> 성공률이 전반적으로 너무 높다 조금씩 낮추자  +10은 10% +9는 15% +8은 20% 대충 이런 느낌으로
실패 시   = 골드만 소실. 단계 하락 없음, 파괴 없음 (코지 톤 유지)
```

**품질 × 강화의 고점 차이** (말씀하신 "고품질일수록 높은 고점"):

| | +0 | +5 | +10 |
| --- | --- | --- | --- |
| 80% 장비 | 0.80× | 1.20× | 1.60× |
| 100% 장비 | 1.00× | 1.50× | 2.00× |
| 120% 장비 | 1.20× | 1.80× | **2.40×** |

**장비 가격:** `주스탯 × 40 × rarityMult`, `rarityMult = { common:1.0, uncommon:1.25, rare:1.6, epic:2.1, legendary:2.8 }`

### 4.6. MVP에서 뺀 것

| 뺀 것 | 이유 | 언제 |
| --- | --- | --- |
| 환생 / 가보 | 상실감 + 걸음 게이트로 이미 진행이 느림 | 안 넣음 |
| 오프라인 방치 보상 | 걸음의 의미를 지움 | 안 넣음 |
| 가챠 / 천장 / 조각 | 톤 불일치 + 확률 공개 법적 의무 | 안 넣음 |
| **친구 리더보드** | 출시 전엔 볼 친구가 없음 | **T28 (출시 직후 1순위)** |
| 글로벌 랭킹 | 어뷰징 3줄과 세트 | T29 (§11 Q1) |
| 길드 / 레이드 | 콜드스타트 + 운영 부담 | DAU 1,000 이후 |
| 던전 | 기획 미비 | T21 (기획 선행) |
| 펫 / 타일 미니게임 / i18n | 코어 루프에 기여 없음 | 언제든 |
| 직업 5개 × 스킬트리 5티어 | 분량이 게임 하나 크기 | MVP는 3직업 × 스킬 5개, 트리 없음 |

---

## 5. 기술 스택과 아키텍처

### 5.1. 확정 스택

```
런타임      Expo (dev build)
언어        TypeScript strict
라우팅      expo-router
상태        Zustand
저장        react-native-mmkv (+ zod 스키마 검증)          ← T3 완료
걸음(확정)   react-native-health-connect
걸음(실시간) expo-sensors Pedometer.watchStepCount         ← 신규 (#6)
애니메이션   react-native-reanimated
결제        react-native-purchases (RevenueCat)
광고        react-native-google-mobile-ads (AdMob)
분석        @react-native-firebase/analytics
백엔드      Supabase (Auth + Postgres)                     ← Firebase에서 변경 (#8, §3.4)
테스트      vitest                                         ← T3 완료
```

> 버전 숫자는 이 문서에 박지 않습니다. `npx expo install`이 맞춰주는 버전을 따릅니다.

### 5.2. 디렉토리 구조

```
walking_rpg/
├── app/                        # expo-router
│   ├── _layout.tsx             ✅ T2
│   ├── (tabs)/
│   │   ├── index.tsx           ✅ T2  모험 (마을/지역/사냥터)
│   │   ├── character.tsx       ✅ T2  스탯 · 장비 · 인벤토리
│   │   ├── shop.tsx            ✅ T2  상점 · 여관 · 강화 · 창고
│   │   └── settings.tsx        ✅ T2
│   └── battle.tsx              ⬜ T8  전투 (풀스크린, 탭 밖)
│
├── src/
│   ├── game/                   # ★ 순수 TS. React 의존 0. 테스트 대상
│   │   ├── formulas.ts         ⬜ T7  모든 수식 한 곳에
│   │   ├── battle.ts           ⬜ T7  전투 시뮬레이션 (이벤트 배열 반환)
│   │   ├── wp.ts               ⬜ T5  WP 지갑 (지급/소비/자정)
│   │   ├── progression.ts      ⬜ T9  레벨/EXP/스탯/지역 해금
│   │   └── economy.ts          ⬜ T14 골드/상점/강화/창고/드랍
│   │
│   ├── content/                # ★ 게임 데이터 (§7)
│   │   ├── schema.ts           ⬜ T10
│   │   ├── index.ts            ⬜ T10 로더 + 런타임 검증
│   │   └── data/
│   │       ├── regions.json          # 지역 + 사냥터
│   │       ├── monsters/region-0N.json
│   │       ├── items/{weapons,armor,accessory,consumable}.json
│   │       ├── drops.json
│   │       └── skills.json
│   │
│   ├── save/                   ✅ T3
│   │   ├── schema.ts           # 세이브 구조 + version
│   │   ├── store.ts            # MMKV read/write
│   │   ├── migrations.ts       # v1→v2→... 체인
│   │   └── migrations.test.ts
│   │
│   ├── health/
│   │   └── steps.ts            ⬜ T4  HC + 센서 이중 소스 (§3.3)
│   │
│   ├── stores/                 ⬜ Zustand
│   ├── ui/                     ✅ T2  Text/Button/Panel/Bar + theme.ts
│   └── services/               ⬜ ads.ts, purchases.ts, analytics.ts, sync.ts
│
├── tools/
│   ├── validate-content.ts     ⬜ T10
│   ├── gen-content.ts          ⬜ T11
│   └── simulate.ts             ⬜ T12
│
└── assets/sprites | bgm | sfx | fonts(✅ Galmuri11)
```

핵심은 **`src/game/`과 `src/content/`가 React를 전혀 모른다**는 점입니다.
덕분에 전투/밸런스를 Node에서 수만 번 돌려볼 수 있습니다(T12).

### 5.3. 세이브 스키마 로드맵

| 버전 | 시점 | 추가되는 것 |
| --- | --- | --- |
| v1 | ✅ T3 | player(레벨/골드/HP), version |
| v2 | T5 | `wp`, `stepsToday`, `lastGrantAt`, `lastMidnightGrantAt` |
| v3 | T9 | `hpUpdatedAt`, `vault`(창고), 스탯 포인트 |
| v4 | T13 | `inventory: ItemInstance[]`, `equipped` — **품질/강화 때문에 인스턴스 구조 필요 (#25)** |

마이그레이션 체인은 T3에 이미 있으므로 각 단계에서 함수 하나씩만 추가하면 됩니다.

### 5.4. Health Connect 연동

```
앱 실행 / 새로고침 → HC에서 "오늘 00:00 ~ 지금" 걸음 합계 1회 읽기
앱 켜져 있는 동안   → Pedometer.watchStepCount로 실시간 증가분
                    → todaySteps = max(HC, 센서기준선 + 실시간증가분)
                    → 증가분만큼 WP 지급 (단조 증가)
```

- 백그라운드 권한 **불필요**, 포그라운드 서비스 **불필요**, 배터리 영향 **없음**
- **[새로고침] 버튼 필수** — 삼성헬스 → HC 동기화에 수 분 지연 -> 자동으로 안되나? 게임 들어가서 새로고침 버튼 누르는거 별론데
- 설치 이전 걸음은 지급하지 않음 (설치 시점의 오늘 누적을 기준선으로 저장)
- HC 미설치/권한 거부 시 폴백: 안내 화면 + **센서만으로도 플레이 가능** (자정 1,000 WP + 앱 켜놓고 걸은 만큼)

### 5.5. 서버 통신 정책 (#29, T25 이후 적용)

```
전송 시점 = 앱 포그라운드 진입 시 1회
스로틀    = 마지막 전송 후 5분 이내면 건너뜀
접속 중   = 별도 주기 전송 없음. 세이브 변경은 로컬에 쌓아두고 백그라운드 전환 시 1회 flush
-> 백그라운드 전환시 보내는거 가능해? 뒤로가기 눌러서 게임 끌때도 가능해? 킬떄와 끌때 둘다 서버로 보내면 좋은데 끌떄 서버로 보내는게 가능할지 모르겠네  뒤로 눌러서 끌때와 앱들 쫙 켜서 모두 닫기같은거 해서 닫아버릴때도 서버로 보낼 수 있을지
```

v1의 "전 유저 5분마다 전송"은 읽기 폭발 패턴이라 채택하지 않습니다.

---

## 6. 밸런스

> 이 숫자들은 **T12 시뮬레이터로 검증할 초안**입니다.
> 전부 `src/game/formulas.ts` 한 파일에 모여 있어야 수정이 쉽습니다.

### 6.1. 기준선

```
하루 걸음      = 10,000보  (#32)
하루 WP        = 10,000 + 1,000 = 11,000
조우 비용(r)   = 300 × 1.1^(r-1)
하루 조우 수   = 11,000 / 조우비용(r)
                 r1:36.7  r2:33.3  r3:30.3  r4:27.5  r5:25.0
```

### 6.2. EXP와 레벨 곡선 (#32 역산 결과)

```
EXP(r, t)          = 10 × 1.4^(r-1) × (1 + 0.18 × t)        # t = 몬스터 티어 1~5
필요EXP(L → L+1)   = 260 + 20.5 × L^1.35 × 1.0125^L
```

`260` 상수항이 초반을 붙잡고, `1.0125^L` 항이 후반을 늘립니다. 이 두 개가 요청하신 곡선의 핵심입니다.

**필요 EXP 샘플**

| Lv | 1 | 5 | 10 | 20 | 30 | 40 | 49 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 다음 레벨까지 | 281 | 452 | 780 | 1,760 | 3,196 | 5,162 | 7,469 |

**하루 EXP (적정 지역 / 평균 티어 3)**

| 지역 | 1 | 2 | 3 | 4 | 5 |
| --- | --- | --- | --- | --- | --- |
| 조우당 EXP | 15.4 | 21.6 | 30.2 | 42.3 | 59.2 |
| 하루 EXP | 565 | 719 | 915 | 1,164 | 1,482 |

**결과 — 요청하신 목표와 대조 (#32)**

| 목표 | 요청 | 이 수식의 결과 | 누적 EXP |
| --- | --- | --- | --- |
| Lv10 | 1주 | **7.0일** | 4,200 |
| Lv30 | 1.5개월 | **44.9일** | 39,700 |
| Lv50 | 4개월 | **120.1일** | 142,800 |

세 목표 모두 오차 1% 이내입니다. Lv30부터 Lv50까지가 전체 기간의 **63%**를 차지해서,
v2에서 지적하신 "30까지는 빠른데 30~50이 너무 길다" 문제도 해소됐습니다(v2는 그 구간이 80%였습니다).

**덜 걷는 날의 체감** (이게 무너지면 게임이 망합니다)

| 하루 걸음 | 조우 수(r3) | 하루 EXP | Lv20→21 소요 (필요 1,760) |
| --- | --- | --- | --- |
| 0보 | 3회 | 83 | 21.2일 — "걸어야겠다" |
| 4,000보 | 14회 | 416 | 4.2일 |
| **10,000보** | **30회** | **915** | **1.9일** |
| 15,000보 | 44회 | 1,330 | 1.3일 |

### 6.3. 골드

```
골드(r, t) = 40 × 1.45^(r-1) × (1 + 0.15 × t)
```

| 지역 | 1 | 3 | 5 |
| --- | --- | --- | --- |
| 조우당 골드(t=3) | 58 | 122 | 256 |
| 하루 골드 | 2,127 | 3,695 | 6,421 |
-> 1지역과 5지역의 골드가 3배정도밖에 차이안나 1지역이 2000이면 2지역은 4000 3은 6000 4는 8000 5는 10000 정도로 돈을 벌었으면 좋겠어  지역별로 버는 돈 차이가 나긴해야해
그리고 이 골드에 맞춰서 아이템이나 물약, 강화같은 가격도 생각해야해

**지역 1 하루 수지 예시**

```
수입   전투 2,127골드 + 드랍 판매 ~300골드 = 약 2,430골드
지출   물약(소) 4개 240 / 여관 1회 150 / 창고 수수료 ~40
남음   약 2,000골드 → common 장비 1~2부위 또는 강화 3~4회
```

강화로 +10까지 가는 데 실패 포함 기댓값 약 **43,000골드 ≈ 20일치**(tier1 common 무기 기준, 기대 시도 16.3회). 장기 골드 싱크로 충분합니다.

### 6.4. 하루 세션 길이 검증 (#1)

```
지역 3 기준: 30조우 × 20틱 × 1.2초 = 720초 = 12분
+ 마을/상점/인벤토리/강화 조작 3~5분
= 15~17분   ← 목표 10~20분 달성
```

---

## 7. 콘텐츠 데이터 파이프라인

### 7.1. 원칙: 숫자는 공식이, 이름은 AI가, 검증은 스크립트가

```
손으로 만드는 것   원형(archetype) 12개 + 지역/사냥터 정의      ← 여기만 사람이 함
자동 생성하는 것   티어 × 지역 × 등급 스케일링으로 수백 개 파생  ← tools/gen-content.ts
AI로 채우는 것     이름, 설명 문구, 스프라이트                   ← 일괄 생성
반드시 검증        zod + 참조 무결성 + 밸런스 불변식             ← tools/validate-content.ts
```

### 7.2. 원형(archetype)을 실제로 어떻게 쓰는지 (#30)

> "어떤 식으로 만들라는 건지 예시 좀"

**원형은 "숫자"가 아니라 "성격"만 정의합니다.** 숫자는 지역/티어가 결정합니다.

**① 손으로 쓰는 것 — 몬스터 원형 (이런 걸 12개만 씁니다)**

```jsonc
// src/content/archetypes/monsters.json
{
  "id": "arch_beast",
  "label": "야수형",
  // statBias의 합이 원형끼리 동일합니다(= 파워 예산 동일).
  // 그래서 어떤 원형이든 "세기"는 같고 "모양"만 다릅니다.
  -> 아니야 파워 예산도 다 달라야지
  "statBias": { "hp": 1.00, "atk": 1.15, "def": 0.80, "spd": 1.25 },
  "cri": 0.10, "eva": 0.08,
  "traits": ["beast"],
  "namePool": ["들개", "잿빛 늑대", "굶주린 살쾡이", "검은 멧돼지"],
  "spriteTag": "beast"
}
{
  "id": "arch_golem",
  "label": "바위형",
  "statBias": { "hp": 1.45, "atk": 0.95, "def": 1.40, "spd": 0.60 },
  "cri": 0.02, "eva": 0.01,
  "traits": ["construct"],
  "namePool": ["돌무더기", "모래 골렘", "이끼 낀 석상"],
  "spriteTag": "golem"
}
// arch_insect / arch_undead / arch_bandit / arch_spirit / arch_bird ... 총 12개
```

**② 손으로 쓰는 것 — 지역 정의 (5개)**

```jsonc
// src/content/data/regions.json
{
  "id": 3, "name": "메마른 협곡", "levelRange": [16, 26],
  "baseHP": 420, "baseATK": 38, "baseDEF": 14, "baseSPD": 16,   // ← 이 4개만 사람이 감으로 정함
  "fields": [
    { "id": "f_r3_riverbed", "name": "마른 강바닥",
      "pool": [["arch_beast", 2], ["arch_insect", 3], ["arch_golem", 1]] },  // [원형, 티어]
    { "id": "f_r3_cliff", "name": "무너진 절벽길",
      "pool": [["arch_bird", 3], ["arch_bandit", 4], ["arch_golem", 3]] }
  ],
  "boss": { "arch": "arch_golem", "tier": 6, "name": "모래 골렘" }
}
```

**③ 스크립트가 만드는 것 (`gen-content.ts`)**

```ts
hp  = region.baseHP  * 1.35 ** tier * arch.statBias.hp
atk = region.baseATK * 1.28 ** tier * arch.statBias.atk
def = region.baseDEF * 1.28 ** tier * arch.statBias.def
spd = region.baseSPD * 1.10 ** tier * arch.statBias.spd
exp  = 10 * 1.4  ** (region.id - 1) * (1 + 0.18 * tier)
gold = 40 * 1.45 ** (region.id - 1) * (1 + 0.15 * tier)
```

**④ 나오는 결과 (`monsters/region-03.json`)**

```jsonc
{
  "id": "mon_r3_insect_t3", "name": "모래 전갈", "region": 3, "tier": 3,
  "arch": "arch_insect", "sprite": "insect_03",
  "hp": 1034, "atk": 79, "def": 26, "spd": 24,
  "cri": 0.06, "eva": 0.12,
  "exp": 30, "gold": [104, 141],
  "traits": ["insect"]
}
```

**결론: 원형 12개 + 지역 5개 = 손으로 쓰는 건 17개 오브젝트.** 여기서 몬스터 83종이 나옵니다.
그리고 **`region.baseHP` 하나만 고치면 그 지역 전체 난이도가 움직입니다.** 이게 이 방식의 전부입니다.

장비도 똑같습니다:

```jsonc
{ "id": "arch_wpn_sword", "slot": "weapon", "statBias": { "atk": 1.0 },
  "namePool": { "common": ["무뎌진 검"], "rare": ["기사의 장검"], "epic": ["룬이 새겨진 대검"] } }
```
→ `slot 6종 × tier 10 × rarity 5종 = 300개` 장비 자동 생성.
-> 장비와 몬스터 지역도 너가 만들어 줄 순 없나

### 7.3. 파일 분리

```
src/content/
├── archetypes/{monsters,items}.json   # 손으로 쓰는 원형 (17개 오브젝트)
└── data/                              # gen-content.ts가 만드는 것 (커밋함)
    ├── regions.json
    ├── monsters/region-0N.json
    ├── items/{weapons,armor,accessory,consumable}.json
    ├── drops.json
    └── skills.json
```

**drops를 몬스터 안에 안 넣고 분리하는 이유:** 드랍률만 만지는 일이 제일 잦은데,
몬스터 파일 안에 있으면 매번 몬스터 정의를 건드려 diff가 지저분해집니다.

### 7.4. 검증 (`tools/validate-content.ts`)

1. zod 스키마 통과
2. 참조 무결성 — drops의 itemId 존재, field의 arch 존재, boss가 monsters에 존재
3. ID 중복 없음
4. **밸런스 불변식** — 지역 N 최고 장비 < 지역 N+1 최저 장비, 몬스터 HP 단조 증가, 원형별 파워 예산 동일
5. 스프라이트 파일 존재 확인

커밋 전 1초면 돌고, 데이터 사고의 90%를 여기서 잡습니다.

### 7.5. AI 에셋을 "수작업 없이" 쓰는 법 (#10)

Q2에서 "수작업 다듬기도 안 하고 그냥 쓰기"가 목표라고 하셨습니다. 그게 가능하려면 **생성 시점에 규격을 강제**해야 합니다.

```
고정 프롬프트 템플릿  "16-bit pixel art, {spriteTag}, side view facing right,
                      32x32, {팔레트 16색 HEX 나열}, transparent background,
                      no outline glow, flat shading"
팔레트 고정          16색 팔레트 하나를 정하고 모든 프롬프트에 박음  ← 톤 통일의 90%가 여기서 결정됨
후처리 (자동 스크립트) 니어리스트 리샘플 32x32 → 팔레트 강제 양자화 → 배경 알파 컷
```

마지막 줄은 `sharp` 20줄짜리 스크립트로 자동화됩니다. **이게 "수작업"을 없애는 핵심입니다.**
톤이 안 맞는 이유는 대부분 "AI가 색을 자기 마음대로 써서"인데, 팔레트 강제 양자화가 그걸 해결합니다.

**판단 시점:** T11에서 몬스터 20종 뽑아보고 결정하세요. 그때도 별로면 (b) 유료 에셋팩으로 전환합니다.

---

## 8. 구현 단계 — 태스크 목록

> **질문 3번 답변:** "T4 진행해"만으로 **됩니다.** 단, 조건이 있습니다.
>
> 매번 제가 문서 전체를 다시 읽고 해석하면 (a) 시간이 들고 (b) 해석이 미묘하게 달라집니다.
> 그래서 **각 태스크에 "만들 파일 / 완료 기준 / 검증 명령 / 주의사항"을 박아뒀습니다.**
> `T4 진행해` 라고 하시면 저는 §9의 T4 블록만 읽고 시작합니다. 프롬프트를 길게 쓰실 필요 없습니다.
>
> **긴 프롬프트를 매번 붙여넣는 것보다, 문서가 프롬프트인 편이 낫습니다.** 문서는 버전 관리가 되니까요.

### 진행 현황

| 스테이지 | 태스크 | 상태 |
| --- | --- | --- |
| **S0 기반** | T1 Expo + TS strict + ESLint/Prettier + EAS dev build | ✅ **완료** |
| | T2 4탭 라우팅 + 픽셀 테마 + 공용 컴포넌트 3개 | ✅ **완료** |
| | T3 세이브 계층 (zod + version + MMKV + 마이그레이션) | ✅ **완료** |
| **S1 걸음→WP** | T4 Health Connect + 센서 이중 소스 | ⬜ **다음** |
| | T5 `wp.ts` 지갑 | ⬜ |
| | T6 모험 탭 HUD | ⬜ |
| **S2 전투** | T7 `formulas.ts` + `battle.ts` 순수 엔진 | ⬜ |
| | T8 전투 화면 (이벤트 재생) | ⬜ |
| | T9 보상 정산 / 레벨업 / 사망 / 창고 데이터 | ⬜ |
| **S3 콘텐츠** | T10 content 스키마 + 로더 + validate | ⬜ |
| | T11 gen-content + 지역 1~2 실제 데이터 | ⬜ |
| | T12 밸런스 시뮬레이터 | ⬜ |
| **S4 성장·경제** | T13 인벤토리 + 장착 + 품질 (세이브 v4) | ⬜ |
| | T14 상점 + 여관 + 창고 + 물약 | ⬜ |
| | T15 장비 강화 | ⬜ |
| | T16 지역/사냥터 진행 + 보스 + 연속 사냥 | ⬜ |
| | T17 스탯 수동 배분 + 재분배 | ⬜ |
| **S5 살 붙이기** | T18 직업 3종 + 스킬 5개 | ⬜ |
| | T19 일일 목표 + 도감 | ⬜ |
| | T20 로컬 알림 | ⬜ |
| | T21 던전 — **기획 선행 필요** | ⬜ |
| **S6 수익화·출시** | T22 RevenueCat | ⬜ |
| | T23 AdMob 리워드 (일 3회) | ⬜ |
| | T24 Analytics 이벤트 세트 | ⬜ |
| | T25 Supabase Auth + 클라우드 세이브 | ⬜ |
| | T26 온보딩 | ⬜ |
| | T27 출시 준비 (§12 체크리스트) | ⬜ |
| **S7 소셜 (출시 후)** | T28 친구 추가 + 친구 리더보드 + 어뷰징 3줄 | ⬜ |
| | T29 글로벌 랭킹 (§11 Q1 답변 필요) | ⬜ |
| | T30 길드 (DAU 1,000 이후) | ⬜ |

> **🔖 T6 완료 = "만보기 부분" 완성.** 실기기로 하루 들고 다녀보면 됩니다.
> **🔖 T9 완료 = 플레이 가능한 최소 게임.** 걷기→전투→성장 루프가 전부 돕니다.
> **여기서 반드시 일주일 직접 플레이하고 템포를 조정하세요.** 이후 작업은 전부 이 위에 쌓습니다.

### 기간 추정

| 구간 | AI 활용 시 | 비고 |
| --- | --- | --- |
| S1 걸음→WP | 1~2일 | T4는 실기기 대기가 병목 |
| S2 전투 | 2~3일 | 순수 TS라 가장 빠른 구간 |
| S3 콘텐츠 | 2~3일 | 공식 생성이라 분량 대비 빠름 |
| S4 성장·경제 | 4~6일 | CRUD성 UI가 많음 |
| S5 살 붙이기 | 4~6일 | 던전 기획 시간 별도 |
| S6 수익화·출시 | 5~8일 | 결제/광고는 실기기 테스트 대기가 병목 |
| **코드 소계** | **3~4주** | |

**AI로 줄지 않는 것:** 픽셀 아트 톤 통일 / 실제로 걸어보는 밸런싱 **최소 1~2주** / Play Console 심사 며칠~2주 / 실기기 QA.

> **현실적 목표: 코드 3~4주 + 에셋·밸런싱·심사 3~4주 = 약 2개월.**

---

## 9. 태스크 실행 블록 (제가 읽는 부분)

### 공통 규칙 — 모든 태스크에 적용

```
1. 작업 전 v3의 해당 섹션을 먼저 읽는다.
2. 새 의존성 추가는 반드시 먼저 물어본다. npx expo install만 사용.
3. 끝나면 반드시 통과: npm run typecheck && npm run lint && npm test
4. src/game/, src/content/ 는 React를 import하지 않는다. (Node에서 돌아야 함)
5. 수식은 전부 src/game/formulas.ts 에만 둔다. 다른 파일에 숫자를 박지 않는다.
6. 세이브 스키마를 바꾸면 반드시 migrations.ts에 함수 1개 + 테스트 1개를 추가한다.
7. 커밋 메시지: feat(T{N}): {한글 요약}
8. 실기기 확인이 필요하면 MY-TASKS.md에 항목을 추가하고 알려준다.
9. 막히거나 애매하면 추측해서 만들지 말고 물어본다.
```

### T4 — Health Connect + 센서 이중 소스
```
읽을 것   §3.3, §5.4
만들 것   src/health/steps.ts
          app/(tabs)/index.tsx 에 오늘 걸음 표시 + [새로고침] 버튼
추가 의존 react-native-health-connect, expo-sensors  (설치 전 확인받을 것)
          app.json에 ACTIVITY_RECOGNITION 권한, Health Connect 플러그인
완료 기준 실기기에서 실제 걸음 수가 화면에 뜬다. 앱 켜놓고 걸으면 숫자가 즉시 오른다.
검증      npm run typecheck && npm run lint && npm test
주의      백그라운드 권한 쓰지 않음.
          HC 미설치 / 권한 거부 상태에서 앱이 죽지 않아야 함 (안내 화면 + 센서 폴백).
          todaySteps는 max(HC, 센서)로 단조 증가. 절대 감소하지 않을 것.
내가 할 것 EAS 리빌드 필요 (네이티브 모듈 추가). 폰에서 HC 권한 허용. 1,000보 걷고 대조.
```

### T5 — WP 지갑
```
읽을 것   §4.1
만들 것   src/game/wp.ts, src/game/wp.test.ts
          세이브 스키마 v2 (wp, stepsToday, lastGrantAt, lastMidnightGrantAt) + 마이그레이션
완료 기준 유닛 테스트 통과: 재실행 / 시계 앞뒤 조작 / 자정 경계 / 중복 지급 / 걸음 감소
주의      보유 상한 없음. 자정 1,000은 "마지막 수령 시각의 단조 증가"로 판정.
          시계를 뒤로 돌려도 추가 지급이 없어야 하고, 앞으로 돌려도 1회만 지급.
```

### T6 — 모험 탭 HUD
```
읽을 것   §4.1, T2의 src/ui/*
만들 것   app/(tabs)/index.tsx 상단 HUD: 오늘 걸음 / WP / HP / 골드
          src/stores/usePlayer.ts (Zustand)
완료 기준 걷고 새로고침하면 WP가 오른다. 앱 재시작해도 값이 유지된다.
주의      기존 src/ui/Bar.tsx 재사용. 새 컴포넌트 만들지 말 것.
```

### T7 — 전투 엔진 (순수 함수)
```
읽을 것   §4.2, §4.3, §6
만들 것   src/game/formulas.ts, src/game/battle.ts, src/game/battle.test.ts
          tools/battle-bench.ts (전투 1000회 → 승률/평균 틱/무승부율 출력)
완료 기준 node로 전투 1,000회 돌려 승률·평균 틱수·무승부율이 출력된다.
          적정 레벨 기준 평균 10~20틱이 나온다.
주의      React import 금지. 난수는 주입받는 형태(seed 가능)로 만들 것 — 테스트 재현성 때문.
          반환값은 이벤트 배열: { tick, actor, type, value, hpAfter }[]  + outcome
          outcome: 'win' | 'lose' | 'flee' | 'timeout'
          최대 60틱 무승부 반드시 구현 (§3.6 ① — 이게 빠지면 탱커가 무적이 됨).
          판정 순서: 회피 → 크리 → 대미지, 최소 1 보장.
```

### T8 — 전투 화면
```
읽을 것   §4.2
만들 것   app/battle.tsx
완료 기준 T7의 이벤트 배열을 1.2초에 1개씩 재생. 데미지 팝업 + 전투 로그 + [도망] 버튼.
주의      전투 결과는 T7이 이미 다 계산해 둔 것을 "재생"만 한다. 화면에서 계산하지 않는다.
          도망은 재생을 중단하고 outcome을 flee로 바꾼다 (HP는 그 시점 값 보존).
```

### T9 — 보상 정산
```
읽을 것   §4.2 결과 4종, §4.3, §4.5 창고
만들 것   src/game/progression.ts + 테스트
          세이브 스키마 v3 (hpUpdatedAt, vault, statPoints) + 마이그레이션
완료 기준 전투 → 보상 → 레벨업 → 저장 한 바퀴 완주. 앱 껐다 켜도 유지.
주의      HP 자연회복(10분당 1%)은 여기서 계산. 경과 음수면 0, 1회 최대 24시간.
          사망 시 창고 골드는 면제.
```

### T10~T30
```
착수 시점에 이 블록을 같은 형식으로 채웁니다.
(앞 태스크 결과에 따라 파일 목록이 달라지므로 미리 확정하면 오히려 틀립니다)
```

---

## 10. 검증 계획

### 10.1. 자동 테스트 (코어만)

```bash
npm run typecheck   # tsc --noEmit
npm run lint        # expo lint
npm test            # vitest run --dir src   (src/game, src/save, src/content만)
npm run validate    # tools/validate-content.ts  (T10부터)
npm run sim         # tools/simulate.ts          (T12부터)
```

| 대상 | 무엇을 확인 |
| --- | --- |
| `wp.ts` | 자정 경계, 중복 지급, 앱 재실행, **기기 시계 앞/뒤로 돌리기**, 걸음 감소 |
| `battle.ts` | 승률 분포, 평균 틱 10~20, **무승부율 5% 미만**, 대미지 최소 1, 도망/사망 경로 |
| `progression.ts` | EXP 곡선 단조 증가, HP 자연회복 경계(음수/24h 초과), 사망 시 창고 면제 |
| `economy.ts` | 강화 비용 오버플로우, 골드 음수 불가, 창고 수수료 반올림 |
| `migrations.ts` | v1 세이브 → 최신 스키마 무손실 변환 ✅ |
| 콘텐츠 | 참조 무결성, ID 중복, 지역 간 파워 단조성, 원형 파워 예산 동일 |

> UI 단위 테스트는 만들지 않습니다. 비용 대비 효과가 낮고 실기기 확인으로 충분합니다.

### 10.2. 수동 검증

- **걸음 정확도**: 1,000보 걷고 삼성헬스 수치와 비교 (±2% 이내)
- **워치 케이스**: 갤럭시워치 차고 배드민턴 → HC값이 센서값보다 커도 정상 지급되는지
- **동기화 지연**: 걷고 즉시 vs 5분 후 새로고침 비교
- **전투 템포**: 지하철에서 한 손으로 가능한가. 20틱이 지루한가/짧은가
- **하루 세션 길이**: 실제 측정. **목표 10~20분.** 넘으면 조우 비용 상향
- **일주일 체류**: 7일 연속 플레이하며 "내일도 켤 이유"가 있는지
- **저사양 확인**: 보급형 단말에서 전투 애니메이션 30fps 이상
- **권한 거부 경로**: HC 거부/미설치에서도 앱이 죽지 않는지

---

## 11. 답이 필요한 질문

> 각 질문에 제 추천을 달아뒀습니다. **"전부 추천대로"** 한 마디면 넘어갑니다.

**Q1. 글로벌 랭킹, 넣을까요?**
친구 리더보드(T28)는 확정입니다. 그 위에 전체 랭킹을 얹을지가 질문입니다.
- (a) **친구 리더보드만.** 조작 위험 0, 구현 1/3 — **추천**
- (b) 둘 다. 어뷰징 3줄(§3.2)이 필수로 따라옴
- (c) 나중에 T28 써보고 결정

**Q2. 직업 3종 이름/컨셉, 이대로 갈까요?**
전사(버팀) / 도적(속공·크리) / 마법사(한 방). — **추천: 이대로.** 도트로 실루엣 구분이 쉽습니다.
다른 안 있으시면 지금 말씀해 주세요 (T18에서 바꾸면 스프라이트를 다시 뽑아야 합니다).

**Q3. 창고 입출금을 마을에서만 하게 할까요? (§3.8)**
- (a) **마을에서만.** 2% 수수료가 의미 있는 선택이 됨 — **추천**
- (b) 아무 데서나. 편하지만 창고가 매번 누르는 잡일이 됨
-> 위에서도 말했지만 마을에서만 가능함. 넣을때 2%수수료. 창고에 넣을 수 있는 돈 제한 있음. 제한 늘리는것도 골드로 삼

**Q4. WP 보유 상한 (§3.7)**
- (a) **상한 없음 + 연속 사냥 1회 20전투 제한** — **추천** (요청하신 그대로 + 방치 차단)
- (b) 상한 100,000 WP (약 9일치)
- (c) 상한 없음, 연속 사냥 제한도 없음 (사실상 방치형 허용) -> c로

**Q5. 장비 품질 분포 (#25)**
- (a) **삼각분포** — 100% 근처가 흔하고 80%/120%가 드묾. "좋은 게 나왔다"는 순간이 생김 — **추천**
- (b) 균등분포 — 120%가 쉽게 나와서 특별함이 없음

**Q6. 스탯 재분배 비용**
- (a) **3,000 WP, Lv10 이전 무료** — **추천**
- (b) 항상 무료 (밸런스 실수를 아예 무마)
- (c) 더 비싸게 (10,000 WP)

**Q7. T25(클라우드 세이브)까지 세이브가 날아가는 게 걱정되시나요?**
- (a) **괜찮다, T25까지 기다린다** — **추천** (지금은 테스터가 본인 한 명)
- (b) T3.5로 "세이브 파일 내보내기/가져오기"를 지금 붙여달라 (1시간, 백엔드 0)

**Q8. 던전(T21) 기획, 언제 할까요?**
- (a) **S5 진입 직전에 별도 세션** — **추천**
- (b) 지금 같이 잡자
- (c) MVP에서 완전히 뺀다

**Q9. 다음 작업, T4로 갈까요?**
T4는 네이티브 모듈이 추가돼서 **EAS 리빌드 + 실기기 재설치**가 필요합니다.
- (a) **T4 먼저.** 리빌드 한 번 하고 걸음이 뜨는 걸 보는 게 동기부여가 큼 — **추천**
- (b) T7(전투 엔진) 먼저. 리빌드 없이 Node에서 끝까지 가능. 재미있는 부분을 먼저 봄
- (c) T5(WP 지갑) 먼저. 리빌드 없이 가능하고 T4 없이도 테스트 가능

---

## 12. 출시 체크리스트

- [ ] **개인정보처리방침 URL** — 건강 데이터를 읽으므로 필수
- [ ] **Play Console 데이터 안전성 양식** — 걸음 데이터 수집/전송 여부 정확히 기재
- [ ] **Health Connect 사용 선언** — Play 정책상 접근 사유 설명 필요. **반려 사유 1순위**
- [ ] **ACTIVITY_RECOGNITION 권한 설명** — 센서 사용 사유 (#6 때문에 추가됨)
- [ ] **권한 사전 고지 화면** — 왜 걸음 데이터가 필요한지 설명 후 요청 (승인율 차이 큼)
- [ ] **연령 등급** — Play 자체 설문으로 GRAC 등급 부여
- [ ] **확률형 아이템 없음 확인** — 가챠를 넣는다면 확률 공개 의무 발생
- [ ] **구독 상품 심사 요건** — 가격/주기/해지 방법을 앱 내에 명시
- [ ] **스토어 자산** — 아이콘, 스크린샷 4장 이상, 짧은 설명
- [ ] **내부 테스트 트랙** → 비공개 테스트 → 프로덕션

---

## 13. v2 → v3 변경 요약 (상세)

### 13.1. 이름 / 용어

| 항목 | v2 | v3 |
| --- | --- | --- |
| 걸음 화폐 이름 | 기력(氣力) | **WP (Walking Point)** |
| 코드 심볼 | `stamina`, `src/game/stamina.ts` | `wp`, `src/game/wp.ts` |
| "나침반" | v1 잔재, 설명 없이 언급됨 | **완전 삭제** (§3.5에 뭐였는지만 설명) |

### 13.2. WP 경제 (전면 수정)

| 항목 | v2 | v3 | 이유 |
| --- | --- | --- | --- |
| 전투 1회 비용 | 1,000 WP | **몬스터 조우당 300 × 1.1^(r-1)** | 하루 플레이 10~20분 목표 (#1, #13) |
| 사냥터 입장 | 1,000 WP (= 전투 1회) | **무료** | 사냥터는 "방", 조우가 과금 단위 (#13) |
| 지역 이동 | 2,000 WP | **1,000 WP** | (#15) |
| 지역별 비용 계수 | 1.25^(r-1) | **1.1^(r-1)** | 1.25는 후반 체감이 너무 가파름 (#29 of v2) |
| 보유 상한 | 30,000 WP | **없음** | (#2) — 부작용은 연속 사냥 20전투 제한으로 대응 |
| 자정 기본 지급 | 1,000 | **1,000 (유지)** | (#4) |
| 하루 행동 수(10,000보) | 11회 | **36회** | 3.3배 증가 |
| 하루 세션 길이 | 목표 3~6분 | **목표 10~20분 (실측 15~17분)** | (#1) |

### 13.3. 전투 (수식 전면 확정)

| 항목 | v2 | v3 |
| --- | --- | --- |
| 대미지 | `max(1, ATK × mult - DEF × 0.5) × rand(0.9~1.1)` (감산 방식) | **`ATK × 50/(50+DEF) × rand(0.8~1.2)`** (비율 감산, K=50) (#20) |
| 크리티컬 | `random() < CRI` (CRI 정의 없음) | **CRI = 독립 스탯(확률), CRD = 배율.** 기본 5% / 1.5× (#21) |
| 회피 | 없음 | **EVA 스탯 추가.** 판정 순서 = 회피 → 크리 → 대미지 (#22) |
| 전투 길이 | "20~40틱 고정" | **목표 밴드로만 표현.** 지역1 10~20틱 → 지역5 18~32틱 (#17) |
| 최대 틱 | 없음 | **60틱 → 무승부(`timeout`).** 보상 없음, WP 환불 없음 (신규, §3.6) |
| 결과 종류 | win / lose / flee | **win / lose / flee / timeout** |
| 사망 시 HP | HP 1 | **최대 HP의 10%** (HP 1이면 바로 또 죽음) |
| 사망 페널티 | 소지 골드 10% | **소지 골드 10%, 창고 골드 면제** (#11, #19) |

### 13.4. HP (신규 규칙)

| 항목 | v2 | v3 |
| --- | --- | --- |
| 자연 회복 | **없음** (물약/여관만) | **10분당 최대 HP의 1%** (#18) |
| 오프라인 회복 | — | **적용됨** (`hpUpdatedAt` 기준 경과 계산) |
| 시계 조작 방어 | — | 경과 음수면 0, 1회 계산 최대 24시간 |
| 물약 | 소 50골드 HP 40% / 중 180골드 HP 70% | **소 60골드 +60 / 중 180골드 +200 / 대 480골드 +600 (절대값)** + **엘릭서 1,200골드 50%** (#24) |
| 여관 | 지역별 80~500골드 | **150 × 1.45^(r-1)** (r1:150 → r5:663) |

### 13.5. 스탯과 성장 (전면 재설계)

| 항목 | v2 | v3 |
| --- | --- | --- |
| 스탯 배분 | "직업별 자동 분배" (본문) / "수동 분배" (주석) — **모순 상태** | **직업 자동 성장 + 레벨당 3포인트 수동, 병행** (#23, #31) |
| 1차 스탯 | 없음 (HP/ATK/DEF/SPD 직접) | **힘/체력/민첩/행운 → 파생 스탯 매핑** (HP를 직접 못 찍게) |
| 재분배 | 언급만 | **3,000 WP. Lv10 이전 무료** |
| "HP만 찍으면 무적" 문제 | 미해결 (본인이 지적) | **3중 방어: 60틱 무승부 / 1차 스탯 매핑 / 직업 자동 성장** (§3.6) |
| 직업 | 3종 (이름 미정) | **전사 / 도적 / 마법사 + 레벨당 성장 수치 확정** |

### 13.6. 레벨 곡선 (역산 완료)

| 항목 | v2 | v3 |
| --- | --- | --- |
| 필요 EXP | `13 × Lv^1.3` | **`260 + 20.5 × L^1.35 × 1.0125^L`** |
| 기준 걸음 | 8,000보 | **10,000보** (#32) |
| Lv10 도달 | (계산 없음) | **7.0일** (목표 1주) |
| Lv30 도달 | 약 30일 (너무 빠름) | **44.9일** (목표 1.5개월) |
| Lv50 도달 | 약 120일, **그중 Lv30~50이 75%** | **120.1일, Lv30~50이 63%** |
| 누적 EXP | 45,000 | **142,800** |
| 조우당 EXP | `18 × 1.50^(r-1) × (1+t×0.20)` | **`10 × 1.4^(r-1) × (1+0.18t)`** |
| 조우당 골드 | `110 × 1.45^(r-1) × (1+t×0.15)` | **`40 × 1.45^(r-1) × (1+0.15t)`** |

> v2의 문제였던 "30까지 3일인데 30~50이 4개월"이 해소됐습니다. 상수항 `260`이 초반을 붙잡고 `1.0125^L`이 후반을 늘립니다.
> **이 계수는 손으로 잡은 게 아니라 요청하신 7일/45일/120일을 목표로 수치 최적화해서 뽑았고, 실제로 돌려 검산했습니다.**

### 13.7. 장비 (신규 시스템 2개)

| 항목 | v2 | v3 |
| --- | --- | --- |
| **품질** | 없음 | **드랍/구매 시 80~120% 랜덤 (삼각분포).** 같은 등급·같은 장비도 개체차 (#25) |
| **강화 효과** | "+N 강화" (효과 불명) | **`기본 × 품질 × (1 + 0.10 × N)`, 상한 +10** (#26) |
| 강화 비용 | `base × 1.6^N` | 유지. `장비가격 × 0.3 × 1.6^N`으로 구체화 |
| 강화 성공률 | 명시 없음 | **+3까지 100%, 이후 90%→30% 하향.** 실패 시 골드만 소실 |
| 인벤토리 저장 | 아이템 ID | **인스턴스 `{uid, defId, quality, enhance}`** — 세이브 v4 마이그레이션 필요 |
| 장비 가격 | `주스탯 × 15 × rarityMult` | **`주스탯 × 40 × rarityMult`** (골드 싱크 강화) |

### 13.8. 지역 / 사냥터 구조

| 항목 | v2 | v3 |
| --- | --- | --- |
| 계층 | 지역 → 전투 | **지역 → 사냥터 → 조우** (#14) |
| 사냥터 | 숫자만 명시 (3~4개) | **각 사냥터마다 몬스터 풀(원형+티어) 정의** |
| 몬스터 수 | 미정 | **78종 + 보스 5종** (사냥터당 3~5종) |
| 보스 | 언급만 | **조우 비용 1,000 × 1.1^(r-1), 재도전 가능** |
| 연속 사냥 | 없음 | **신규.** WP 부족 / HP 30% / 인벤 가득 / 20전투 에서 자동 정지 |
| 창고 | 없음 | **신규.** 입금 2% 수수료, **마을에서만**, 사망 면제 (#19, §3.8) |

### 13.9. 걸음 수집 (설계 변경)

| 항목 | v2 | v3 |
| --- | --- | --- |
| 소스 | Health Connect 단일 | **HC(확정) + 센서(실시간) 이중** (#6) |
| 합산 규칙 | — | **`todaySteps = max(HC, 센서기준선 + 실시간증가분)`** — 단조 증가 |
| 추가 의존성 | — | `expo-sensors` (Pedometer), `ACTIVITY_RECOGNITION` 권한 |
| 워치/배드민턴 오탐 | 미논의 | **"두 값을 비교해 벌주는 로직을 안 만든다"로 원천 제거** (§3.2) |
| HC 거부 시 | 자정 1,000만으로 플레이 | **센서만으로도 플레이 가능** (앱 켜놓고 걸은 만큼) |

### 13.10. 백엔드 / 소셜

| 항목 | v2 | v3 |
| --- | --- | --- |
| 백엔드 | Firebase (Auth + Firestore) | **Supabase (Auth + Postgres)** (#8) |
| 구글 드라이브 백업 | 미논의 | **§3.4에서 비교 후 탈락.** 소셜 확장이 안 돼서 |
| 친구 리더보드 | "리더보드 = MVP 제외" | **T28로 확정.** 출시 직후 1순위 (#27, #28) |
| 글로벌 랭킹 | MVP 제외 | T29, **§11 Q1로 질문** |
| 길드 | DAU 1,000 이후 | **유지.** 이유는 §3.1에 상세 |
| 어뷰징 방지 | "랭킹 넣으면 필수"만 언급 | **§3.2에 구체적 3줄 + 난이도 답변** (#5) |
| 서버 전송 주기 | 5분마다 | **포그라운드 진입 시 1회 + 5분 스로틀** (#29) |

### 13.11. 콘텐츠 파이프라인

| 항목 | v2 | v3 |
| --- | --- | --- |
| 원형(archetype) | 개념만 ("10~15개 만드세요") | **§7.2에 실제 JSON + 생성 결과까지 전부 작성** (#30) |
| 파워 예산 | 없음 | **`statBias` 합이 원형끼리 동일.** 원형은 "세기"가 아니라 "모양"만 정의 |
| 손으로 쓰는 양 | 불명확 | **원형 12개 + 지역 5개 = 17개 오브젝트** → 몬스터 83종 |
| 조정 지점 | 불명확 | **`region.baseHP` 하나로 그 지역 전체 난이도가 움직임** |
| AI 에셋 | (a)/(b)/(c) 선택지 | **(a) 확정 + §7.5에 "수작업 없이 톤 통일하는 법"** (#10) |
| 에셋 후처리 | 없음 | **팔레트 16색 고정 + `sharp` 자동 양자화 스크립트** |

### 13.12. 태스크 목록

| 항목 | v2 | v3 |
| --- | --- | --- |
| 총 태스크 | 26개 (T1~T26) | **30개 (T1~T30)** |
| 실행 방식 | 표만 있음 | **§9에 태스크별 실행 블록** (읽을 것/만들 것/완료 기준/검증/주의) (질문 3) |
| 공통 규칙 | 없음 | **§9 상단에 9개 규칙** (의존성 승인, React 금지 경계, 수식 위치 등) |
| 진행 표시 | 없음 | **T1~T3 ✅ 완료 표시** (질문 2) |
| 신규 태스크 | — | T17(스탯 배분), T21(던전, 기획 선행), T28~T30(소셜) |
| 백엔드 태스크 | T24 Firebase | **T25 Supabase** |
| 삭제/이동 | T18 요일 던전이 MVP 안 | **T21로 이동 + "기획 미비" 명시** (#16) |

### 13.13. 문서 구조

| 항목 | v2 | v3 |
| --- | --- | --- |
| 질문 답변 | §4에 Q1~Q4 | **§3 전체를 "물어보신 것에 대한 답변"으로.** 8개 항목 |
| 모순 지적 | §3 (v1의 모순) | **삭제.** v1은 이제 2세대 전이라 의미 없음 |
| 미해결 질문 | §4 (4개) | **§11 (9개), 전부 추천안 포함** |
| 밸런스 근거 | 숫자만 제시 | **목표 → 역산 과정 → 결과 대조표** |

---

## 부록 A. 원본 기획서

- [만보기 RPG 기획 고도화.txt](만보기%20RPG%20기획%20고도화.txt)
- `implementation_plan.v2.md` — 본 파일로 대체되었습니다 (코멘트 원본 보존용으로 남겨둠)

-> 맨 마지막에 v3->4변경 요약 있어야해 상세하게
-> 애매한것은 나에게 물어봐