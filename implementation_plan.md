# StepQuest — 구현 계획서 v2

> 현실의 걸음으로 움직이는, 느린 도트 감성 RPG (Android 전용)

---

## 0. 이 문서에 대하여

v1 계획서에 달아주신 `->` 코멘트 10개를 전부 반영해 **전면 재작성**한 버전입니다.

코멘트 중 몇 개(“슬로우한 게임”, “걸음 의존적”, “방치보다 횟수 제한”)는 단순 수정이 아니라
**게임의 코어 루프 자체를 바꾸는 요구**였습니다. 그래서 문구만 고치지 않고 설계를 다시 했습니다.

읽는 순서 추천:
1. **§1 게임 한 줄 정의 + 하루 시나리오** — 바뀐 게임이 어떤 모습인지
2. **§2 반영표** — 내 코멘트가 어디로 갔는지
3. **§3 모순 & 누락** — 요청하신 지적 사항 (v1의 치명적 문제 포함)
4. **§9 구현 단계** — 에이전트한테 던질 작업 목록

---

## 1. 게임 한 줄 정의

> **걸어서 번 “기력”으로 하루 5분, 몬스터 몇 마리 잡고, 천천히 강해지는 도트 RPG.**

### 1.1. 하루 플레이 시나리오 (목표: 1일 3~6분)

```
08:20  출근길. 앱 안 켬. 폰은 주머니에서 걸음만 셈.
19:40  퇴근길 지하철. 앱 실행.
       ├ "오늘 8,412보 → 기력 8,412 획득" (+ 자정 기본 지급 1,000)
       ├ HP 62/180. 어제 싸우다 남은 체력 그대로.
       ├ 여관에서 자면 120골드, 물약 2개 사면 100골드. 물약 선택.
       ├ [사냥터: 바람 부는 초원] 입장 (기력 1,000)
       │   40초짜리 전투. 공격 → 피격 → 데미지 숫자 → 로그.
       │   "늑대에게 24의 피해를 입혔다"
       │   HP 30 남음 → [도망] 눌러서 후퇴. 보상 없음.
       ├ 물약 마시고 다시 입장. 이번엔 승리. 골드 140, EXP 18, 가죽 조각 1.
       ├ 3~4번 더 반복 → 레벨업 → 스탯 상승
       └ 남은 기력 4,000은 내일로 이월 (보유 상한 30,000)
19:46  앱 종료.
```

핵심은 **“못 걸으면 못 논다”** 입니다. 시간이 흐른다고 캐릭터가 강해지지 않습니다.

### 1.2. 설계 원칙 4개

| 원칙 | 의미 | 이걸로 잘려나간 것 |
|---|---|---|
| **걸음이 유일한 수도꼭지** | 모든 성장은 전투를 거치고, 전투는 기력을 쓰고, 기력은 걸음에서 나온다 | 시간 방치 보상, 오프라인 누적 골드 |
| **하루 5분** | 하루에 할 수 있는 행동 수 자체가 제한됨 | 24시간 자동 사냥, 일일 퀘스트 10종 |
| **천천히** | 레벨 50까지 약 4개월. 급가속 구간 없음 | 환생, 급성장 가챠 |
| **고전 도트 RPG** | 마을/사냥터/여관/상점. 지역은 레벨로 해금 | 걸음 수에 따른 실시간 배경 전환 |

---

## 2. 내 코멘트 반영표

| # | 내 코멘트 | 반영 결과 | 위치 |
|---|---|---|---|
| 1 | iOS 추가 예정 없음 | **Android 단독.** CoreMotion / HealthKit / StoreKit / Apple 로그인 전부 삭제 | §6 |
| 2 | 픽셀 아트로 생각중 | 확정. 에셋 제작 방식은 여전히 열린 질문 | §4-Q2 |
| 3 | 서버 장단점 궁금 / 서버리스로 길드 되나? | **§6.1 전체를 이 질문 답변에 할애.** 결론: 된다, 그리고 MVP엔 서버가 아예 필요 없다 | §6.1 |
| 4 | AdMob + RevenueCat | 확정. `react-native-iap` → `react-native-purchases`로 교체, 영수증 검증 코드 삭제(RevenueCat이 처리) | §6.2 |
| 5 | 어뷰징 기능 안 넣음 | StepValidator / validateSteps / 케이던스 분석 전부 삭제. **단, 랭킹을 넣는 순간 문제가 생김 → §3-C에서 지적** | §3-C |
| 6 | “스텝 파워” 이름 별로 / 뭐에 쓰는지? / 1걸음=1 | **이름: 기력(氣力) 제안.** 1보 = 1기력. 용처 3개를 §5.1에 명시 | §5.1 |
| 7 | 바이옴 3,000보마다 전환은 이상함 / 슬로우한 게임 / 하트 같은 횟수 재화 | 걸음-바이옴 연동 **삭제**. 지역은 레벨+보스로 해금. 기력이 곧 “하트” 역할 | §5.1, §5.3 |
| 8 | 환생 고민됨 / 상실감 | **MVP에서 삭제.** 출시 후 옵션으로만 남김 (§5.5) | §5.5 |
| 9 | 가보 시스템 모르겠고 필요한가 | **삭제.** 환생이 없으면 가보는 존재 이유 자체가 없음 | §5.5 |
| 10 | 전투는 자동이되 느린 템포 / 위험하면 도망 / HP는 자연회복 말고 물약 | **§5.2 전체 재설계.** 1초에 1행동, HP 영구 지속, 물약·여관으로만 회복 | §5.2 |
| 11 | AI 써도 이렇게 오래 걸리나? | **§9.6에서 재산정.** 코드는 1/4로 줄지만 에셋·밸런싱·심사는 안 줄어듦 | §9.6 |
| 12 | 아이템/몬스터 많이 만들어야 함, 데이터 파일 분리 | **§7 전체를 콘텐츠 파이프라인에 할애.** 파일 구조 + 대량 생성 방법 | §7 |

---

## 3. v1 계획의 모순과 누락 (요청하신 지적)

### 3.1. 치명적 모순

#### A. 방치 보상이 걸음을 무의미하게 만든다 ★ 가장 큰 문제

v1의 밸런스 시트(§6.3)가 스스로 증명합니다.

```
하루 수입 (v1 기준)
  자동 전투 24시간 ...... 50,000 골드   ← 폰을 주머니에 넣어두면 들어옴
  걸음수 골드(10,000보) .. 1,000 골드   ← 2시간 걸어야 들어옴
```

**만보기 게임인데 걷기가 전체 수입의 2%입니다.** 게임의 전제가 무너져 있습니다.
v2는 시간 기반 수입을 **0으로** 만들었습니다. 걷지 않으면 아무 일도 일어나지 않습니다.

#### B. “짧은 플레이타임”과 콘텐츠 분량이 충돌

v1이 MVP에 넣은 것: 스킬트리(5직업×5티어), 타일 미니게임, 요일보스, 길드 채팅,
길드 레이드, 리더보드 5종, 가챠 2종+천장+조각, 환생, 가보, 합성.
**이건 하루 30분짜리 라이브 서비스 게임의 분량입니다.** “지하철에서 한 손으로”와 양립 불가.
v2는 MVP를 4탭(모험/캐릭터/상점/설정)으로 줄였습니다.

#### C. 어뷰징 방지를 뺐는데 랭킹은 남아 있다

- “일간 걸음수 랭킹”, “길드 주간 100만보 챌린지”가 계획에 그대로 있습니다.
- 검증이 0이면 **출시 첫날 상위 100명은 전부 흔들이 기계** 입니다. 정상 유저는 즉시 이탈합니다.
- **v2의 해법: 랭킹과 길드를 MVP에서 뺐습니다.** 순위가 없으면 조작할 동기도 없습니다.
- 나중에 랭킹을 넣으실 거면, 그때 최소 한 줄(일일 상한 + 서버 시간 기준 증가량 체크)은 반드시 필요합니다. 이건 “어뷰징 시스템”이 아니라 랭킹의 전제 조건입니다.

#### D. 바이옴이 매일 자정에 초기화된다

v1은 “오늘 걸음 8,000보 → 얼어붙은 봉우리”입니다.
그러면 **매일 새벽 0시에 화산에서 초보자 숲으로 강등**됩니다. 진행이라는 개념이 성립하지 않습니다.
v2: 지역은 캐릭터 레벨 + 지역 보스 클리어로 **영구 해금**.

#### E. 스텝 레벨의 보상이 방향과 반대

v1: “스텝 레벨이 오르면 최대 방치 시간 4h → 24h”.
많이 걸을수록 **덜 걸어도 되는 보상**을 줍니다. 원하시는 방향과 정반대입니다.
v2: 스텝 레벨 삭제. 걸음은 기력 하나로만 환원.

#### F. 일일 나침반 상한 10개 = 10,000보 초과분 버림

v1은 10,000보에서 보상이 끊깁니다. 15,000보 걷는 헤비 유저에게 **5,000보는 0원**입니다.
v2: 획득량에 상한 없음. 대신 **보유 상한**(3일치)만 둬서 무한 적립을 막습니다.

#### G. 가챠가 코지 게임과 안 맞고, 법적 부담까지 생김

- 천장 90회 / 조각 150개는 “느긋한 게임”의 톤이 아닙니다.
- 더 중요한 건: 확률형 아이템을 넣는 순간 **게임산업법상 확률 공개 의무**(2024.3 시행) 대상이 되고,
  확률 표기·게임 내 고지·광고 표기까지 따라옵니다. 1인 개발 MVP에서 감당할 필요가 없습니다.
- v2: **가챠 없음.** 장비는 전부 드랍/상점/강화로 획득. 나중에 넣더라도 출시 후 별도 과제.

### 3.2. 누락 (v1에 아예 없던 것들)

| # | 누락 항목 | 왜 문제인가 | v2 대응 |
|---|---|---|---|
| 1 | **Health Connect 백그라운드 제약** | v1은 “5분 주기 서버 전송”을 가정. Android에서 백그라운드 헬스 읽기는 별도 권한(Android 14+/HC 1.1)과 포그라운드 서비스가 필요 | 앱 열 때만 그날 누적을 읽음. 백그라운드 불필요 → 권한/배터리/복잡도 전부 제거 (§6.4) |
| 2 | **Expo Go로 못 돌아감** | Health Connect / RevenueCat / AdMob / MMKV 전부 네이티브 모듈. Expo Go 불가 | T1에서 **EAS dev build**부터 세움 (§9) |
| 3 | **세이브 스키마 버전 / 마이그레이션** | 데이터 주도 게임인데 저장 포맷이 바뀌면 기존 유저 세이브가 깨짐 | T3에서 `version` 필드 + 마이그레이션 함수 체인 |
| 4 | **캐릭터 사망 시 처리** | HP가 자연회복 안 되는데 죽으면 어떻게 되는지 정의가 없음 | §5.2에 사망 페널티 명시 |
| 5 | **분석(Analytics)** | v1은 D1 40% / D7 20%를 목표로 잡았는데, 측정 수단이 하나도 없음 | T23 Firebase Analytics 이벤트 세트 |
| 6 | **클라우드 세이브 / 기기 변경** | Phase 1~2가 로컬 전용 → 폰 바꾸면 전부 소실. 테스터도 마찬가지 | T24 (익명 로그인 + 세이브 1문서) |
| 7 | **법·스토어 요건** | 개인정보처리방침(필수), Play 데이터 안전성 양식, 건강 데이터 사용 선언 | §11 출시 체크리스트 |
| 8 | **기기 시계 조작** | v1의 오프라인 보상은 폰 시계를 하루 앞으로 돌리면 그대로 뚫림 | 방치 보상 자체를 없앴고, 일일 지급은 “마지막 수령 시각의 단조 증가”로 판정 |
| 9 | **걸음 데이터 지연 / 소급** | 삼성헬스 → Health Connect 동기화에 수 분 지연. 설치 이전 걸음 처리 기준 없음 | 수동 새로고침 버튼 + “설치 이후, 당일분만” 규칙 (§6.4) |
| 10 | **오프라인 동작** | 지하철 터널·비행기모드에서 실행되는가에 대한 언급 없음 | 오프라인 우선 설계. 네트워크는 광고/결제/백업에만 필요 |
| 11 | **접근성** | 등급을 색으로만 구분(회/녹/청/보/금/적) → 색약 유저 구분 불가 | 색 + 테두리 문양 + 등급 약어 병기 |
| 12 | **밸런스 검증 수단** | 수식은 있는데 “이 수식으로 놀면 어떤 곡선이 나오는지” 확인할 방법이 없음 | T12 밸런스 시뮬레이터 CLI |
| 13 | **Expo SDK 버전 고정** | v1은 `expo ~52.x`로 박아둠. 계획서 작성 시점에 이미 구버전 | 버전 숫자를 계획서에 박지 않음. 프로젝트 생성 시점 latest 사용 |
| 14 | **길드 콜드스타트** | 최대 30명 길드인데, 출시 초 동접이 수십 명이면 길드가 채워지지 않음 | MVP 제외 |

---

## 4. 아직 답이 필요한 질문

> 아래 4개는 제 추천안을 같이 적어뒀습니다. “추천대로” 한 마디면 넘어갑니다.

**Q1. 기력의 최종 이름**
`기력(氣力)` 추천 — 고전 RPG 톤이고 2글자라 UI에 들어가며, HP(체력)와 헷갈리지 않습니다.
대안: `활력` / `원기` / `발자국` / 그냥 `걸음`.
→ 이 문서는 전부 **기력**으로 쓰겠습니다. 바꾸시면 상수 하나만 고치면 됩니다.

**Q2. 픽셀 아트 에셋 제작 방식** (v1에서 미답변)
- (a) AI 생성(고정 프롬프트 + 팔레트 통일) 후 수작업 다듬기 — 추천. 초기 비용 0, 몬스터 100종도 가능
- (b) 유료 에셋팩 구매 (itch.io, 16x16/32x32 던전팩) — 톤 일관성 최고, 몇만 원, 커스텀 불가
- (c) 외주 — 품질 최고, 가장 느리고 비쌈
- **추천: MVP는 (b)로 빠르게 채우고, 부족한 것만 (a)로 보충.** 에셋 때문에 개발이 멈추는 게 최악입니다.

**Q3. 사망 페널티 강도**
- (a) 골드 10% 상실 + 마을 귀환 — 추천 (고전 RPG 관습, 상실감 낮음)
- (b) 페널티 없음, 그냥 귀환 — 긴장감 0
- (c) 장비 내구도/드랍 — 코지 톤과 충돌

**Q4. 클라우드 세이브를 언제 넣을까**
- (a) 첫 외부 테스터가 붙기 직전 (T24 위치) — 추천
- (b) 처음부터 — 개발 초기에 계정/동기화 디버깅에 시간 낭비
- (c) 출시 후 — 테스터 세이브 날아가면 재테스트 불가

---

## 5. 코어 루프 설계

### 5.1. 기력 — 걸음 화폐

```
1 걸음 = 1 기력                    (읽기 쉽게, 요청하신 1:1)
자정 기본 지급 = 1,000 기력        ("하루에 기본적으로 조금은 생기는" 부분)
획득 상한 = 없음                   (많이 걸으면 많이 얻음)
보유 상한 = 30,000 기력            (약 3일치. 무한 적립 방지)
```

**기력의 용처는 딱 3개입니다** (질문하신 부분):

| 용처 | 비용(1지역 기준) | 설명 |
|---|---|---|
| **사냥터 입장** | 1,000 / 전투 1회 | 주 소비처. 전투는 여기서만 발생 |
| **던전 입장** | 3,000 / 1회 | 요일마다 바뀌는 특수 던전. 좋은 드랍 |
| **지역 이동** | 2,000 / 1회 | 새 지역 첫 진입 시 1회만. 고전 RPG의 “여행” |

> 설계 의도: **기력 = 오늘 몇 번 행동할 수 있는가**, **골드 = 얼마나 강해지는가.**
> 골드는 전투에서만 나오고 전투는 기력을 쓰므로, **모든 성장 경로가 걸음을 통과**합니다.

**하루 행동 횟수 예시**

| 오늘 걸음 | 보유 기력 | 가능한 전투 수 | 체감 |
|---|---|---|---|
| 0보 (비 오는 날) | 1,000 | 1회 | “한 판은 하고 잔다” |
| 3,000보 | 4,000 | 4회 | 가볍게 |
| 8,000보 (목표) | 9,000 | 9회 ≈ 5분 | 기준선 |
| 15,000보 | 16,000 | 16회 | 남는 건 내일로 이월 |

### 5.2. 전투 — 느린 자동 전투

요청하신 “자동이지만 천천히 보이는” 템포입니다.

```
틱 = 1.0초에 1행동 (연출 포함 ~1.2초)
행동 순서 = 민첩 기반 턴 큐
일반 전투 = 20~40틱 ≈ 25~45초

화면에서 보이는 것:
  ├ 캐릭터/몬스터 도트 스프라이트 (좌우 배치, 공격 시 살짝 전진)
  ├ 데미지 숫자가 위로 뜨며 사라짐 (크리티컬은 크게/노랗게)
  ├ 하단 전투 로그 한 줄씩 추가
  │   "늑대의 송곳니에 17의 피해를 입었다"
  │   "치명타! 늑대에게 48의 피해를 입혔다"
  └ 우상단 [도망] 버튼 — 언제든 누를 수 있음
```

**HP 규칙 (요청 반영)**
- HP는 **전투가 끝나도 회복되지 않고 그대로 유지**됩니다. 세이브에 저장됩니다.
- 자연 회복 **없음**. 회복 수단은 둘뿐:
  - **물약** — 상점에서 골드로 구매, 전투 중/밖에서 사용
  - **여관** — 마을에서 골드 지불, HP 전체 회복 (고전 RPG 감성 + 골드 소비처)
- 결과: **골드가 곧 지속력**이고, 골드는 전투에서만 나오고, 전투는 기력을 쓰고, 기력은 걸음에서 나옵니다.

**도망 / 사망**

| 상황 | 결과 |
|---|---|
| 도망 (플레이어 선택) | 전투 보상 없음. 기력 환불 없음. **HP는 남은 만큼 보존** |
| 사망 (HP 0) | 마을 귀환. **소지 골드 10% 상실**. HP 1로 회복. 장비 손실 없음 |

**전투 수식 (초안 — §8에서 수치 조정)**
```
damage      = max(1, ATK × skillMult - DEF × 0.5) × random(0.9 ~ 1.1)
critical    = random() < CRI  →  damage × CRD
turnOrder   = SPD 높은 순, 동률이면 플레이어 우선
escapeRate  = 1.0  (도망은 항상 성공. 실패시키면 스트레스만 늘어남)
```

### 5.3. 성장과 지역

```
캐릭터 레벨   ← 전투 EXP로만 상승. 스탯 포인트 자동 분배(직업별 성장률)
지역(Region) ← 고정 순서. 해금 조건 = 직전 지역 보스 클리어 + 권장 레벨 도달
```

**MVP 지역 5개** (걸음 수와 무관, 영구 해금)

| # | 지역 | 권장 Lv | 사냥터 | 보스 |
|---|---|---|---|---|
| 1 | 바람 부는 초원 | 1~8 | 3 | 들개 우두머리 |
| 2 | 이끼 낀 동굴 | 8~16 | 3 | 동굴 거미 여왕 |
| 3 | 메마른 협곡 | 16~26 | 4 | 모래 골렘 |
| 4 | 잿빛 화산길 | 26~38 | 4 | 화염 드레이크 |
| 5 | 서리 봉우리 | 38~50 | 4 | 서리 군주 |

지역이 올라가면 전투당 기력 비용과 보상이 함께 상승합니다(§8).

### 5.4. 경제

```
수입: 전투 승리 골드 + 드랍 아이템 판매
지출: 물약 / 여관 / 장비 구매 / 장비 강화

물약  (소)  50골드   HP 40% 회복
물약  (중) 180골드   HP 70% 회복
여관 숙박           지역별 80~500골드, HP 전체 회복
장비 강화           +N 강화, 비용 = base × 1.6^N, 실패 시 강화 단계 유지(하락 없음)
```

강화 실패가 단계를 떨어뜨리지 않는 이유: 코지 톤 유지. 손실은 골드만.

### 5.5. MVP에서 뺀 것과 그 이유

| 뺀 것 | 이유 | 나중에? |
|---|---|---|
| **환생 / 가보** | 직접 말씀하신 상실감 문제 + 걸음 게이트라 이미 진행이 느려서 시간 늘릴 장치가 불필요 | Lv50 도달 유저가 생기면 재검토 |
| **오프라인 방치 보상** | §3-A. 걸음의 의미를 지움 | 넣더라도 “기력 선불 원정” 형태로만 (걸음 게이트 유지) |
| **길드 / 레이드** | 콜드스타트로 빈 길드만 생김. 서버 비용·복잡도 대비 효과 낮음 | DAU 1,000 넘으면 |
| **리더보드** | §3-C. 검증 없는 랭킹은 조작판 | 어뷰징 한 줄과 세트로만 |
| **가챠 / 천장 / 조각** | §3-G. 톤 불일치 + 확률 공개 법적 의무 | 출시 후 별도 과제 |
| **어뷰징 검증** | 요청대로 제외. 랭킹이 없어 조작 동기 자체가 없음 | 랭킹 도입 시 필수 |
| **펫 / 타일 미니게임 / i18n** | 코어 루프에 기여 없음 | 언제든 |
| **직업 5개 × 스킬트리 5티어** | 100개 넘는 노드를 설계·밸런싱해야 함. 분량이 게임 하나 크기 | MVP는 **3직업 × 스킬 5개**, 트리 없이 레벨 해금 |

---

## 6. 기술 스택과 아키텍처

### 6.1. Q3 답변 — 서버리스로 길드가 되나? / 서버 필수라며?

**먼저 오해를 풀면: “서버리스”는 서버가 없다는 뜻이 아닙니다.**
Firebase Cloud Functions / Supabase Edge Functions는 **내가 짠 서버 코드가 남의 컴퓨터에서 돌아가는 것**입니다.
서버를 안 빌리는 게 아니라 **서버를 관리(OS, 스케일링, 배포)하지 않는 것**입니다.

따라서 답: **길드·랭킹·가챠 전부 서버리스로 구현 가능합니다.** v1이 “서버 필수”라고 한 것과 모순되지 않습니다.
Cloud Functions가 그 “서버”입니다.

**세 선택지 비교**

| | Firebase | Supabase | 자체 서버 (NestJS 등) |
|---|---|---|---|
| 형태 | 서버리스 + NoSQL(Firestore) | 서버리스 + PostgreSQL | 직접 운영 |
| 길드/랭킹 | ✅ Functions + Firestore | ✅ Edge Functions + SQL | ✅ |
| 랭킹 구현 난이도 | 중 — NoSQL이라 정렬·집계를 직접 설계해야 함 | **하** — `ORDER BY ... LIMIT 100` 한 줄 | 하 |
| 초기 세팅 | **가장 빠름** (Auth/DB/Push/Analytics 한 세트) | 빠름 | 느림 (인증·배포·모니터링 직접) |
| 비용 예측 | **어려움** — 읽기 횟수 과금. 실시간 리스너 남발하면 폭증 | 쉬움 (고정 요금제) | 쉽다 (서버 대여료) |
| 운영 부담 | 없음 | 없음 | **큼** (1인 개발엔 부담) |
| 락인 | 큼 | 작음 (그냥 Postgres) | 없음 |
| 무료 티어 | 넉넉 | 넉넉 | 없음 |

> v1의 “실시간 리스너로 상위 100명 랭킹 + 전 유저 5분마다 걸음 전송” 설계는
> Firestore에서 **읽기 횟수가 폭발하는 대표적인 패턴**입니다. 이 부분은 그래서 삭제했습니다.

**그래서 v2의 결론: MVP는 백엔드가 아예 없습니다.**

지금 설계된 게임은 **1인용 + 오프라인 + 랭킹 없음**입니다. 서버가 할 일이 없습니다.
로컬 저장(MMKV)만으로 완성됩니다. 서버가 필요해지는 시점은 정확히 이때입니다:

| 기능 추가 시 | 필요한 것 |
|---|---|
| 기기 변경 대비 세이브 백업 | **익명 로그인 + 문서 1개 저장** ← 이것만 T24에서 넣음 (Firebase 무료 티어로 충분) |
| 랭킹 | 서버 집계 + 최소 검증 |
| 길드 | 서버 상태 공유 + 트랜잭션 |
| 가챠 | 서버 RNG (클라 RNG는 100% 조작됨) |

**추천 경로: 백엔드 없음 → (T24) Firebase Auth+Firestore 세이브 백업만 → 소셜 넣을 때 재검토.**
지금 Firebase를 깔아두면 나중에 소셜 붙이기 쉽고, 세이브 백업 하나만 쓰면 비용은 사실상 0입니다.

### 6.2. 확정 스택

```
런타임      Expo (dev build, managed 아님 — 네이티브 모듈 필요)
언어        TypeScript strict
라우팅      expo-router
상태        Zustand
저장        react-native-mmkv  (+ zod 스키마 검증)
걸음        react-native-health-connect        ← Android 전용, 단일 소스
애니메이션   react-native-reanimated
결제        react-native-purchases (RevenueCat)  ← 요청 반영
광고        react-native-google-mobile-ads (AdMob) ← 요청 반영
분석        @react-native-firebase/analytics
백업(후기)   @react-native-firebase/auth + firestore
테스트      vitest 또는 jest (코어 로직만)

삭제: expo-sensors(iOS fallback), react-native-iap, ReceiptValidator,
      CoreMotionAdapter, StepValidator, Cloud Functions 일체
```

> **버전은 이 문서에 박지 않습니다.** 프로젝트 생성 시점의 Expo latest SDK를 쓰고
> 나머지 패키지는 `npx expo install`이 맞춰주는 버전을 따릅니다.

### 6.3. 디렉토리 구조

```
walking_rpg/
├── app/                        # expo-router
│   ├── _layout.tsx
│   ├── (tabs)/
│   │   ├── adventure.tsx       # 마을/지역/사냥터 선택  (메인)
│   │   ├── character.tsx       # 스탯 · 장비 · 인벤토리
│   │   ├── shop.tsx            # 상점 · 여관 · 강화
│   │   └── settings.tsx
│   └── battle.tsx              # 전투 (풀스크린, 탭 밖)
│
├── src/
│   ├── game/                   # ★ 순수 TS. React 의존 0. 테스트 대상
│   │   ├── battle.ts           # 전투 시뮬레이션 (틱 단위, 순수 함수)
│   │   ├── stamina.ts          # 기력 지갑 (지급/소비/상한/자정 처리)
│   │   ├── progression.ts      # 레벨/EXP/스탯/지역 해금
│   │   ├── economy.ts          # 골드/상점/강화/드랍
│   │   └── formulas.ts         # 모든 수식 한 곳에
│   │
│   ├── content/                # ★ 게임 데이터 (§7)
│   │   ├── schema.ts           # zod 스키마
│   │   ├── index.ts            # 로더 + 런타임 검증
│   │   └── data/
│   │       ├── regions.json
│   │       ├── monsters/       # region-01.json ~ region-05.json
│   │       ├── items/          # weapons.json armor.json accessory.json consumable.json
│   │       ├── drops.json
│   │       └── skills.json
│   │
│   ├── save/
│   │   ├── schema.ts           # 세이브 구조 + version
│   │   ├── store.ts            # MMKV read/write
│   │   └── migrations.ts       # v1→v2→... 마이그레이션 체인
│   │
│   ├── health/
│   │   └── healthConnect.ts    # 권한 + 오늘 걸음 읽기 (이 파일 하나가 전부)
│   │
│   ├── stores/                 # Zustand
│   │   ├── usePlayer.ts
│   │   └── useBattle.ts
│   │
│   ├── ui/                     # 공용 컴포넌트 + 픽셀 테마 토큰
│   └── services/               # ads.ts, purchases.ts, analytics.ts
│
├── tools/
│   ├── validate-content.ts     # 콘텐츠 스키마 검증 (CI/커밋 훅)
│   ├── gen-content.ts          # 원형 → 대량 생성
│   └── simulate.ts             # 밸런스 시뮬레이터 (§9 T12)
│
└── assets/sprites | bgm | sfx | fonts
```

핵심은 **`src/game/`과 `src/content/`가 React를 전혀 모른다**는 점입니다.
덕분에 전투/밸런스를 Node에서 수만 번 돌려볼 수 있습니다(T12).

### 6.4. Health Connect 연동 — 현실적인 방법

v1은 “5분마다 서버 전송”을 가정했는데, Android에서 이건 백그라운드 헬스 권한 + 포그라운드 서비스 + 배터리 최적화 예외가 필요합니다. **그런데 우리 게임은 그럴 필요가 없습니다.**

```
앱 실행 → Health Connect에서 "오늘 00:00 ~ 지금" 걸음 합계 1회 읽기
        → 마지막 반영 걸음 수와 비교
        → 증가분만큼 기력 지급
        → 끝
```

- 백그라운드 권한 **불필요**, 포그라운드 서비스 **불필요**, 배터리 영향 **없음**
- 하루에 여러 번 켜도 중복 지급 없음(마지막 반영값 기준 차이만 지급)
- 앱 안에 **[새로고침]** 버튼 필수 — 삼성헬스→Health Connect 동기화에 수 분 지연이 있음
- 설치 이전 걸음은 지급하지 않음 (설치 시점의 오늘 누적을 기준선으로 저장)
- Health Connect 미설치/권한 거부 시 폴백: 기능 안내 화면 + 기본 지급 1,000기력만으로 플레이 가능

---

## 7. 콘텐츠 데이터 파이프라인 (몬스터 100종 / 아이템 300종을 어떻게 만드나)

> 요청하신 “아이템·몬스터 데이터를 파일로 분리” + “이 많은 걸 어떻게 만들지”에 대한 답입니다.

### 7.1. 원칙: 숫자는 공식이, 이름은 AI가, 검증은 스크립트가

```
손으로 만드는 것   원형(archetype) 10~15개 + 지역 5개 정의        ← 여기만 사람이 함
자동 생성하는 것   티어별 스케일링으로 몬스터/장비 수백 개 파생    ← tools/gen-content.ts
AI로 채우는 것     이름, 설명 문구, 스프라이트 프롬프트            ← 일괄 생성 후 붙여넣기
반드시 검증        zod 스키마 + 참조 무결성 + 밸런스 시뮬레이션   ← tools/validate-content.ts
```

**왜 이 순서인가:** 수백 개를 손으로 만들면 밸런스가 반드시 깨집니다.
공식으로 만들면 지역 3의 장비가 지역 4보다 센 사고가 구조적으로 불가능합니다.

### 7.2. 파일 분리 (요청하신 부분)

```
src/content/data/
├── regions.json              # 지역 5개: 이름, 권장레벨, 기력비용, 보스, 해금조건
├── monsters/
│   ├── region-01.json        # 지역별로 파일 분리 → 한 파일이 커지지 않음
│   ├── region-02.json
│   └── ...
├── items/
│   ├── weapons.json          # 무기
│   ├── armor.json            # 투구 / 상의 / 장갑 / 신발  (slot 필드로 구분)
│   ├── accessory.json        # 장신구
│   └── consumable.json       # 물약 등
├── drops.json                # 몬스터ID → 아이템ID 확률 테이블 (분리해야 밸런싱이 쉬움)
└── skills.json               # 직업 3종 × 스킬 5개
```

**왜 drops를 몬스터 안에 안 넣고 분리하나:** 드랍률만 만지는 일이 제일 잦은데,
몬스터 파일 안에 있으면 매번 몬스터 정의를 건드리게 되고 diff가 지저분해집니다.

### 7.3. 스키마 예시

```typescript
// monsters/region-01.json 의 한 항목
{
  "id": "mon_r1_wolf",
  "name": "들개",
  "region": 1,
  "tier": 2,               // 지역 내 난이도 1~5
  "sprite": "wolf_01",
  "hp": 95, "atk": 14, "def": 4, "spd": 11,
  "exp": 18, "gold": [110, 160],   // 최소~최대
  "traits": ["beast"]              // 속성/특성. 장비 세트 효과 대상
}

// items/weapons.json 의 한 항목
{
  "id": "wpn_r1_shortsword",
  "name": "무뎌진 단검",
  "slot": "weapon",
  "rarity": "common",       // common | uncommon | rare | epic | legendary
  "tier": 1,
  "sprite": "sword_01",
  "stats": { "atk": 8 },
  "price": 120,
  "flavor": "누군가 쓰다 버린 단검. 아직 쓸 만은 하다."
}
```

### 7.4. 생성 공식 (예)

```
몬스터 HP   = baseHP(region) × 1.35^tier
몬스터 ATK  = baseATK(region) × 1.28^tier
장비 주스탯 = baseStat(tier) × rarityMult[rarity]
              rarityMult = { common:1.0, uncommon:1.25, rare:1.6, epic:2.1, legendary:2.8 }
장비 가격   = 주스탯 × 15 × rarityMult
```

이 공식 하나로 `tier 1~10 × rarity 5종 × slot 6종 = 300개` 장비가 즉시 생성됩니다.
**사람은 baseStat 곡선 하나만 조정하면 게임 전체 밸런스가 움직입니다.**

### 7.5. 검증 (`tools/validate-content.ts`)

1. zod 스키마 통과
2. 참조 무결성 — drops의 itemId가 실제로 존재하는가, region의 bossId가 monsters에 있는가
3. ID 중복 없음
4. **밸런스 불변식** — 지역 N의 최고 장비 < 지역 N+1의 최저 장비, 몬스터 HP가 단조 증가
5. 스프라이트 파일 존재 확인

커밋 전 1초면 돌고, 데이터 사고의 90%를 여기서 잡습니다.

---

## 8. 밸런스 초안

> 이 숫자들은 **초안**입니다. T12 시뮬레이터로 돌려보고 실제 걸어보며 조정해야 합니다.
> 전부 `src/game/formulas.ts` 한 파일에 모여 있어야 수정이 쉽습니다.

### 8.1. 수식

```
필요 EXP(Lv)     = 13 × Lv^1.3       # Lv2:32  Lv10:259  Lv30:1,082  Lv50:2,102
                                      # 누적 Lv50까지 약 45,000 EXP
레벨업 스탯      = 직업별 성장률 자동 배분 (HP+12, ATK+3 등)
몬스터 HP(r,t)   = baseHP[r] × 1.35^t
전투 예상 틱수   = 목표 20~40틱        # 너무 짧으면 밋밋, 길면 지루
전투 기력비용(r) = 1,000 × 1.25^(r-1) # r1:1,000  r3:1,563  r5:2,441
전투 골드(r,t)   = 110 × 1.45^(r-1) × (1 + t×0.15)
전투 EXP(r,t)    = 18 × 1.50^(r-1) × (1 + t×0.20)
```

> 지수를 1.3으로 낮게 잡은 이유: 하루 전투 횟수가 **기력으로 고정**되어 있어서,
> 일반 방치형 게임의 가파른 곡선(1.8 이상)을 쓰면 후반에 한 레벨에 몇 달이 걸립니다.
> 걸음 게이트가 이미 속도를 제한하므로 곡선까지 가파를 필요가 없습니다.

### 8.2. 하루 수급 (8,000보 / 지역 2 / tier 2 몬스터 기준)

```
수입
  기력 .......... 8,000(걸음) + 1,000(기본) = 9,000
  전투 횟수 ..... 9,000 ÷ 1,250 = 7회
  골드 .......... 7 × 207 = 1,449골드
  EXP ........... 7 × 38  = 266 EXP

지출
  물약 소 3개 .... 150골드
  여관 1회 ....... 120골드
  남는 골드 ...... 1,179골드 → 장비 구매 또는 강화 1~2회

성장 속도
  Lv 12 → 13 ... 필요 372 EXP ÷ 266 ≈ 1.4일
  Lv 30 부근 ... 지역 4에서 하루 약 350 EXP, 필요 1,082 → 약 3일/레벨
  Lv 50 도달 ... 누적 45,000 EXP → 매일 8,000보 기준 약 4개월
```

**체크포인트:** 하루 7전투 × 35초 = **4분 5초**. 상점·강화 1분 추가 → **하루 5분**. 목표 달성.

**덜 걷는 날의 체감** (이게 무너지면 게임이 망합니다)

| 하루 걸음 | 전투 수 | 하루 EXP | Lv30 기준 레벨업 소요 |
|---|---|---|---|
| 0보 | 1회 | 38 | 28일 — "걸어야겠다"는 압박 |
| 4,000보 | 4회 | 152 | 7일 |
| 8,000보 | 7회 | 266 | 4일 |
| 15,000보 | 12회 | 456 | 2.4일 |

---

## 9. 구현 단계 — 에이전트 작업 목록

> 요청하신 “에이전트한테 단계별로 시킬 것”입니다.
> **1 태스크 = 에이전트 1세션**으로 끝나는 크기로 쪼갰습니다.
> 각 태스크는 앞 태스크만 있으면 시작 가능하고, 끝나면 눈으로 확인할 수 있는 결과가 남습니다.

### S0. 기반 (3 태스크)

| ID | 작업 | 완료 기준 (이게 되면 끝) |
|---|---|---|
| **T1** | Expo 프로젝트 생성, TS strict, ESLint/Prettier, **EAS dev build로 실기기 설치 1회 성공** | 폰에서 앱이 켜지고 “Hello” 보임 |
| **T2** | 4탭 라우팅 + 픽셀 테마 토큰(색/폰트/간격) + 공용 컴포넌트 3개(버튼, 패널, 바) | 탭 4개 전환됨, 도트 폰트 적용됨 |
| **T3** | 세이브 계층: zod 스키마 + `version` + MMKV 읽기/쓰기 + 마이그레이션 체인 + 초기화 | 앱 껐다 켜도 값 유지, 스키마 바꿔도 안 깨짐 |

### S1. 걸음 → 기력 (3 태스크)

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T4** | Health Connect: 권한 요청/거부 플로우, 오늘 걸음 읽기, 미설치 폴백, 새로고침 버튼 | 실기기에서 실제 걸음 수가 화면에 뜸 |
| **T5** | `stamina.ts`: 1보=1기력, 증가분만 지급, 자정 1,000 지급(중복 방지), 보유 상한 30,000 | 유닛 테스트 통과 (재실행/시계조작/자정 경계) |
| **T6** | 모험 탭 상단 HUD: 오늘 걸음 / 기력 / HP / 골드 | 걷고 새로고침하면 기력이 오름 |

> **🔖 여기까지가 “만보기 부분” 완성.** 실기기로 하루 들고 다녀보면 됩니다.

### S2. 전투 (3 태스크) ← 가장 중요

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T7** | `battle.ts` 순수 함수 전투 엔진: 틱 큐, 데미지/크리, 도망, 사망, **결과를 이벤트 배열로 반환** | Node에서 전투 1000회 돌려 승률/평균 틱수 출력됨 |
| **T8** | 전투 화면: T7의 이벤트 배열을 1초에 1개씩 재생. 데미지 팝업, 전투 로그, 도망 버튼 | 느린 템포로 전투가 “보임”. 도망이 동작함 |
| **T9** | 보상 정산: 골드/EXP/드랍, 레벨업, 사망 페널티(골드 10%), HP 영구 저장 | 전투→보상→레벨업→저장 한 바퀴 완주 |

> **🔖 여기가 플레이 가능한 최소 게임(vertical slice).** 걷기→전투→성장 루프가 전부 돕니다.
> **이 시점에서 반드시 일주일 직접 플레이해보고 템포를 조정하세요.** 이후 작업은 전부 이 위에 쌓습니다.

### S3. 콘텐츠 (3 태스크)

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T10** | `content/schema.ts` + 로더 + `tools/validate-content.ts` (스키마/참조/중복/단조성) | 잘못된 JSON 넣으면 에러 메시지로 잡힘 |
| **T11** | `tools/gen-content.ts` + 지역 1~2 데이터(몬스터 20, 장비 60, 드랍) 실제 생성 | 지역 2개를 끝까지 플레이 가능 |
| **T12** | `tools/simulate.ts` 밸런스 시뮬레이터: “하루 N보 유저가 30일 플레이” 시뮬 → 레벨/골드 곡선 출력 | 수식 바꾸면 곡선이 바로 보임 |

### S4. 성장·경제 (4 태스크)

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T13** | 인벤토리 + 6부위 장착/해제 + 스탯 반영 + 정렬/판매 | 장비 바꾸면 전투 결과가 달라짐 |
| **T14** | 상점(장비/소모품) + 여관 + 전투 중 물약 사용 | 골드로 HP를 복구할 수 있음 |
| **T15** | 장비 강화(+N, 비용 1.6^N, 실패해도 유지) | 강화한 장비가 전투에 반영됨 |
| **T16** | 지역 진행: 사냥터 선택, 지역 보스, 해금 조건, 이동 기력 비용 | 지역 1 보스 잡고 지역 2 해금됨 |

### S5. 살 붙이기 (4 태스크)

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T17** | 직업 3종 + 스킬 5개씩(레벨 해금, 트리 없음) + 전투 엔진 연동 | 직업마다 전투 양상이 다름 |
| **T18** | 요일 던전 (기력 3,000, 요일별 몬스터/드랍) | 요일 바뀌면 던전 내용이 바뀜 |
| **T19** | 일일 목표(걸음 목표 달성 보상) + 도감 | 하루 한 번 열어볼 이유가 생김 |
| **T20** | 로컬 알림: “기력이 가득 찼습니다”, “오늘 목표까지 2,000보” | 알림 눌러서 앱 진입됨 |

### S6. 수익화·출시 (6 태스크)

| ID | 작업 | 완료 기준 |
|---|---|---|
| **T21** | RevenueCat: 광고 제거(영구), 기력 부스터 월 구독 / 샌드박스 결제 테스트 | 테스트 계정으로 구매·복원 성공 |
| **T22** | AdMob 리워드 광고: 기력 +2,000 또는 물약, **일 3회 상한** | 광고 보고 보상 받음, 4회째 차단됨 |
| **T23** | Firebase Analytics 이벤트 세트(설치/튜토리얼완료/첫전투/D1복귀/전투수/구매) | GA4 콘솔에 이벤트 들어옴 |
| **T24** | 클라우드 세이브: 익명 로그인 + 구글 연동 + Firestore 문서 1개 백업/복원 | 앱 지우고 다시 깔아도 복원됨 |
| **T25** | 온보딩: 첫 실행 3분 안에 걸음 연동 → 첫 전투 → 첫 레벨업까지 | 처음 켠 사람이 안 막힘 |
| **T26** | 출시 준비 (§11 체크리스트 전부) | 내부 테스트 트랙 업로드 성공 |

### 기간 재산정 (질문하신 “AI 쓰면 얼마나 줄어드나”)

**결론: 코드 작성은 1/3~1/4로 줄지만, 줄지 않는 것이 따로 있습니다.**

| 구간 | v1 추정 | AI 활용 시 | 비고 |
|---|---|---|---|
| S0~S2 (플레이 가능까지) | 4~6주 | **4~7일** | 순수 TS 로직이라 AI가 가장 잘하는 영역 |
| S3 콘텐츠 | (없었음) | **2~3일** | 공식 생성이라 분량 대비 매우 빠름 |
| S4 성장·경제 | 3~4주 | **4~6일** | CRUD성 UI가 많음 |
| S5 살 붙이기 | 3~4주 | **4~6일** | |
| S6 수익화·출시 | 3~4주 | **5~8일** | 결제/광고는 **실기기 테스트 대기 시간**이 병목 |
| **소계 (코드)** | **13~18주** | **3~5주** | |

**AI로 줄지 않는 것:**
- **픽셀 아트 에셋** — 생성은 빨라도 톤 통일·리사이즈·아틀라스 작업은 손일 (Q2 참고)
- **밸런싱** — 실제로 며칠 걸어보며 체감을 봐야 함. 시뮬레이터(T12)가 있어도 **최소 1~2주 실사용**
- **Play Console 심사** — 첫 제출은 며칠~2주. 건강 데이터 선언 때문에 반려 가능성 있음
- **실기기 QA** — 다양한 폰의 Health Connect 동작 차이는 직접 돌려봐야 함

> **현실적 목표: 코드 4주 + 에셋/밸런싱/심사 3~4주 = 약 2개월.**
> 단, S2까지 끝낸 시점(1주차)에 이미 **직접 플레이 가능한 게임**이 나옵니다.
> 그때부터 매일 걸어보면서 나머지를 만드는 게 가장 효율적입니다.

---

## 10. 검증 계획

### 10.1. 자동 테스트 (코어만, 프레임워크 최소)

```bash
npm test          # src/game/ 와 src/content/ 만 대상. UI 테스트 안 함
npm run validate  # tools/validate-content.ts — 커밋 전 필수
npm run sim       # tools/simulate.ts — 밸런스 확인
```

| 대상 | 무엇을 확인 |
|---|---|
| `stamina.ts` | 자정 경계, 중복 지급, 보유 상한, 앱 재실행, **기기 시계 뒤로 돌리기** |
| `battle.ts` | 승률 분포, 평균 틱수 20~40, 데미지 최소 1 보장, 도망/사망 경로 |
| `progression.ts` | EXP 곡선 단조 증가, 레벨업 스탯 누락 없음 |
| `economy.ts` | 강화 비용 오버플로우, 골드 음수 불가 |
| `migrations.ts` | v1 세이브 → 최신 스키마 무손실 변환 |
| 콘텐츠 | 참조 무결성, ID 중복, 지역 간 파워 단조성 |

> UI 단위 테스트(`ProgressBar.test.tsx` 같은 것)는 만들지 않습니다.
> 비용 대비 효과가 낮고, 실기기 확인으로 충분합니다.

### 10.2. 수동 검증

- **걸음 정확도**: 실제로 1,000보 걷고 삼성헬스 수치와 앱 수치 비교 (±2% 이내)
- **동기화 지연**: 걷고 즉시 켰을 때와 5분 후 새로고침 비교 → 안내 문구 필요 여부 판단
- **전투 템포**: 지하철에서 한 손으로 플레이 가능한가. 한 전투가 지루한가/짧은가
- **하루 세션 길이**: 실제 측정. 목표 3~6분을 넘으면 기력 비용을 올릴 것
- **일주일 체류**: 7일 연속 플레이하며 “내일도 켤 이유”가 있는지 직접 판단
- **저사양 확인**: 보급형 단말에서 전투 애니메이션 30fps 이상
- **권한 거부 경로**: Health Connect 거부 / 미설치 상태에서도 앱이 죽지 않는지

---

## 11. 출시 체크리스트 (v1에 없던 부분)

- [ ] **개인정보처리방침 URL** — 건강 데이터를 읽으므로 필수. 무료 호스팅 페이지로 충분
- [ ] **Play Console 데이터 안전성 양식** — 걸음 데이터 수집/전송 여부 정확히 기재
- [ ] **Health Connect 사용 선언** — Play 정책상 건강 데이터 접근 사유 설명 필요. 반려 사유 1순위
- [ ] **권한 사전 고지 화면** — 왜 걸음 데이터가 필요한지 설명 후 권한 요청 (승인율 차이 큼)
- [ ] **연령 등급** — Play 자체 설문으로 GRAC 등급 부여됨
- [ ] **확률형 아이템 없음 확인** — 가챠를 넣는다면 확률 공개 의무가 발생함을 기억할 것
- [ ] **구독 상품 심사 요건** — 가격/주기/해지 방법을 앱 내에 명시
- [ ] **스토어 자산** — 아이콘, 스크린샷 4장 이상, 짧은 설명, 프로모 영상(선택)
- [ ] **내부 테스트 트랙** 먼저 → 비공개 테스트 → 프로덕션

---

## 12. 참고: v1 → v2 변경 요약 (한 장)

```
삭제  방치 보상 · 스텝 레벨 · 환생 · 가보 · 길드 · 레이드 · 리더보드
      가챠/천장/조각 · 어뷰징 검증 · 타일 미니게임 · 펫 · i18n
      iOS 전반(CoreMotion/StoreKit/Apple 로그인) · Cloud Functions · react-native-iap

변경  걸음 → 바이옴 전환      ⇒  걸음 → 기력(행동 횟수)
      24시간 자동 사냥        ⇒  기력을 소비하는 1회성 전투
      HP 자동 회복            ⇒  물약 · 여관으로만 회복 (영구 지속)
      스텝 파워 (1보=2)       ⇒  기력 (1보=1)
      직업 5 × 스킬트리 5티어 ⇒  직업 3 × 스킬 5개 (레벨 해금)
      백엔드 Firebase 필수    ⇒  MVP 백엔드 없음, 나중에 세이브 백업만

추가  느린 템포 전투 연출 · 도망/사망 규칙 · 여관 · 콘텐츠 생성 파이프라인
      밸런스 시뮬레이터 · 세이브 마이그레이션 · 분석 이벤트 · 출시 체크리스트
```

---

## 부록 A. 원본 기획서

- [만보기 RPG 기획 고도화.txt](만보기%20RPG%20기획%20고도화.txt)
- v1 계획서는 본 파일로 대체되었습니다.
